---
phase: 04-agente-conversacional-core
plan: 08
type: execute
wave: 4
depends_on: ["04-03", "04-04", "04-06"]
files_modified:
  - scripts/verify-agent.ts
autonomous: true
requirements: [SEG-10, SEG-11, SEG-05, SEG-06, SEG-07]

must_haves:
  truths:
    - "A low-risk tool call is proven to execute and leave an audit entry, against the real database"
    - "A high-risk tool call is proven NOT to execute and to leave a pending approval plus an audit entry"
    - "A client contact is proven to read zero rows from approval_queue"
    - "A member's agent context is proven to exclude an unassigned client's messages"
    - "A client contact's agent context is proven to contain only their own client's messages"
    - "An unknown sender's agent context is proven empty"
  artifacts:
    - path: "scripts/verify-agent.ts"
      provides: "Real-Neon integration suite for the SEG-10 engine and the SEG-05..08 context boundary"
      min_lines: 350
  key_links:
    - from: "scripts/verify-agent.ts"
      to: "lib/agent/risk-interceptor.ts"
      via: "calls the real classifyAndExecute, never a reimplementation"
      pattern: "classifyAndExecute\\("
    - from: "scripts/verify-agent.ts"
      to: "lib/agent/build-context.ts"
      via: "calls the real buildAgentContext per identity"
      pattern: "buildAgentContext\\("
---

<objective>
Write the real-Neon integration suite for everything this phase added to the
database boundary: the risk engine's two branches, the approval queue's RLS, and
`buildAgentContext`'s scoping for all four identity shapes.

Purpose: Phases 1-3 each learned the same lesson the hard way — TypeScript types
come from the schema file, not from the live database, so a green build proves
almost nothing about RLS. Every prior phase's real bugs (the `''::uuid` crash,
the SEG-12 leak, the partial-index ON CONFLICT failure) were found by a script
exactly like this one, and none were visible to `tsc`.
Output: one script, wired as `npm run db:verify-agent` (registered in 04-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agente-conversacional-core/04-RESEARCH.md
@.planning/phases/04-agente-conversacional-core/04-03-SUMMARY.md
@.planning/phases/04-agente-conversacional-core/04-04-SUMMARY.md
@.planning/phases/04-agente-conversacional-core/04-06-SUMMARY.md

<locked_decisions>
This script does NOT call the LLM. The turn loop's own behaviour is covered by
the live checkpoint (04-12); what is asserted here is the deterministic layer —
classification, queueing, auditing, and RLS — which must be provable without an
API key and without spending tokens on every run.

Outbound WhatsApp sends are stubbed at the `fetch` layer, exactly as
`scripts/verify-whatsapp-send.ts` does it. A verification run must never deliver
a real message to a real phone.
</locked_decisions>

<interfaces>
```typescript
// lib/agent/risk-interceptor.ts (04-06)
export async function classifyAndExecute(
  toolUse: { id: string; name: string; input: unknown }, actor: TurnActor,
): Promise<Anthropic.Messages.ToolResultBlockParam>;
export const TOOL_TO_CATALOG_CODE: Record<string, string>;

// lib/agent/build-context.ts (04-04)
export async function buildAgentContext(agencyId, identity, key: ConversationKey, tools): Promise<AgentContext>;

// lib/agent/tools/index.ts (04-06)
export function toolsFor(identity: ResolvedIdentity): Anthropic.Messages.Tool[];

// lib/agent/audit.ts (04-06)
export async function writeAuditLog(params): Promise<string>;

// Tenant scopes
export async function withResolvedIdentityContext<T>(agencyId, identity, fn): Promise<T>;
export async function withSystemWebhookContext<T>(agencyId, fn): Promise<T>;

// Seeded catalog rows (migration 0008): payment_reminder=low,
// reschedule_appointment=high, new_client_content=high
// Tool map (04-06): send_payment_reminder->payment_reminder,
//                   draft_client_content->new_client_content
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seeding harness and the SEG-05..08 context-scoping assertions</name>
  <read_first>
    - scripts/verify-whatsapp-webhook.ts (the harness to copy verbatim: `failures`, `check()`, `assertRowCount()`, `errorMessage()`, `expectThrows()`, the `batch()` wrapper with its eslint-disable comment, randomized ids, and the `finally` cleanup that deletes the seeded agency)
    - scripts/verify-agent-rls.ts (plan 04-01 — reuse its seeding shape so the two scripts read as siblings, but seed a SEPARATE agency id so the two can run back to back)
    - scripts/verify-whatsapp-send.ts (the `globalThis.fetch` stub and its restoration)
    - lib/agent/build-context.ts (the function under test — read its actual signature)
  </read_first>
  <files>scripts/verify-agent.ts</files>
  <action>
Create `scripts/verify-agent.ts` with the standard harness and a
`seed()` function creating, under one agency `verify-agent-${randomUUID()}`:
- team members: `admin` (role admin), `memberAssigned` (role member),
  `memberOther` (role member), each with a distinct `whatsapp_number`
- clients `clientA`, `clientB`
- `client_assignments`: memberAssigned → clientA
- `authorized_contacts`: `contactA` → clientA with `optInConfirmedByTeam: true`
  and a phone number; `contactB` → clientB with `optInConfirmedByTeam: false`
  (the negative case for SEG-04)
- `messages` rows via `withSystemWebhookContext`: two inbound text rows for
  clientA's contact phone, two for clientB's contact phone, two for a team
  member's phone with `client_id = NULL`

Stub `globalThis.fetch` at the top of `main()` to intercept
`graph.facebook.com` POSTs and return
`{ messages: [{ id: "wamid.verify-" + counter }] }`, recording each call;
restore it in the `finally`. Set the Meta env fixtures the same way
`verify-whatsapp-send.ts` does.

Context-scoping assertions (call the REAL `buildAgentContext`, passing
`toolsFor(identity)`):
1. admin identity + `{channel:"whatsapp", counterpartPhoneNumber: contactA.phone}`
   → `context.messages` is non-empty and contains clientA's message text.
2. memberAssigned identity + clientA's phone → non-empty, contains clientA's
   text.
3. memberAssigned identity + clientB's phone → `context.messages.length === 0`.
   THIS IS THE SEG-06 PROOF at the agent-context level, not just the SQL level.
4. client_contact identity for clientA + clientA's phone → non-empty, and the
   joined text of `context.messages` does NOT contain clientB's message text.
5. client_contact identity for clientA + a team member's phone (the
   team-internal thread) → `context.messages.length === 0`.
6. unknown identity + any phone → `context.messages.length === 0` and
   `context.tools.length === 0`.
7. Every produced `context.system` contains the `AI_DISCLOSURE_RULE` string
   (import the constant and assert containment — SEG-09 at the integration
   level, not only the unit level).
8. `toolsFor(client_contact)` and `toolsFor(unknown)` both return `[]`;
   `toolsFor(team_member)` returns exactly 2 tools (LD-06).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -q 'buildAgentContext(' scripts/verify-agent.ts` exits 0
    - `grep -q 'AI_DISCLOSURE_RULE' scripts/verify-agent.ts` exits 0
    - `grep -q 'globalThis.fetch' scripts/verify-agent.ts` exits 0 and a `finally` restores it
    - `grep -q 'set_config' scripts/verify-agent.ts` returns non-zero (all scopes opened through the real helpers)
    - At least 8 `check(` calls exist for the context-scoping section
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The suite seeds a complete agency, stubs outbound sends, and asserts the agent context boundary for admin, assigned member, unassigned member, client contact and unknown — including the disclosure rule and the empty tool set for non-team identities.</done>
</task>

<task type="auto">
  <name>Task 2: SEG-10 risk-engine and approval_queue RLS assertions</name>
  <read_first>
    - lib/agent/risk-interceptor.ts (the function under test — read the real branch order and return shapes)
    - lib/db/schema/approval-queue.ts and drizzle/migrations/0016_approval_queue.sql (the columns and the three policies being asserted)
    - lib/db/schema/audit-log.ts (the new `status` and `approvalId` columns)
  </read_first>
  <files>scripts/verify-agent.ts</files>
  <action>
Append to the same script (one file, two sections — mirror how
`verify-whatsapp-webhook.ts` groups its sections with `console.log` headers):

Risk-engine assertions, all calling the REAL `classifyAndExecute` with a
`TurnActor` built from the seeded data:
9.  `send_payment_reminder` (low) with `{clientId: clientA, message: "..."}` and
    an admin actor → the returned tool_result has `is_error` falsy, exactly one
    stubbed Graph API POST was made, and a new `messages` row with
    `direction='outbound'` and `client_id = clientA` exists.
10. The same call wrote exactly ONE `audit_log` row with
    `status = 'executed'`, `risk_level = 'low'`,
    `action_type_code = 'payment_reminder'` and `client_id = clientA`.
11. `draft_client_content` (high) with `{clientId: clientA, contentDraft: "..."}`
    → NO additional Graph API POST was made (compare the stub's call count
    before and after). THIS IS THE SEG-10 PROOF.
12. The same call created exactly one `approval_queue` row with
    `status='pending'`, `tool_name='draft_client_content'`,
    `action_type_code='new_client_content'`, `risk_level='high'`, and a
    `tool_input` jsonb that round-trips to the original input.
13. The same call wrote one `audit_log` row with `status='pending_approval'`
    whose `approval_id` equals the queue row's id.
14. Calling `classifyAndExecute` twice with the SAME `tool_use_id` produces
    exactly ONE `approval_queue` row (idempotency).
15. A `client_contact` actor for clientA proposing
    `send_payment_reminder` with `input.clientId = clientB` → the result has
    `is_error: true`, no Graph API POST was made, and no new `approval_queue`
    row exists. THIS IS THE PITFALL-4 / SEG-07 PROOF.
16. An unmapped tool name (`"delete_everything"`) → `is_error: true`, no
    execution, no queue row.
17. `send_payment_reminder` targeting clientB from an admin actor, where
    clientB's only contact has `optInConfirmedByTeam: false` → the tool result
    reports the missing opt-in and NO Graph API POST was made (SEG-04 holds even
    for an admin-initiated agent action).

approval_queue RLS assertions (open each scope through the real helper and
select from `approvalQueue`):
18. admin scope reads the pending row.
19. memberAssigned scope reads it (clientA is assigned).
20. memberOther scope does NOT read it (0 rows).
21. client_contact scope for clientA reads EXACTLY 0 rows. THIS IS THE LD-04
    PROOF.
22. unknown scope reads 0 rows.
23. An UPDATE of `approval_queue.status` from a client_contact scope throws or
    affects 0 rows.

End with the standard summary block and `process.exit(failures.length ? 1 : 0)`,
and a `finally` that deletes the seeded agency (cascade removes everything).
Add a header note that this script requires network egress to Neon and that
migrations 0014-0016 must already be applied.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -c 'check(' scripts/verify-agent.ts` returns at least 23
    - `grep -q 'classifyAndExecute(' scripts/verify-agent.ts` exits 0
    - The script asserts the stubbed Graph API call count is UNCHANGED across the high-risk call (a before/after comparison is present)
    - `grep -q 'pending_approval' scripts/verify-agent.ts` exits 0
    - `grep -q 'approvalQueue' scripts/verify-agent.ts` exits 0
    - `grep -q 'finally' scripts/verify-agent.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const s=fs.readFileSync('scripts/verify-agent.ts','utf8');const n=(s.match(/check\(/g)||[]).length;if(n<23)throw new Error('expected >=23 assertions, found '+n);if(!/classifyAndExecute\(/.test(s))throw new Error('must call the real interceptor');console.log('ok, '+n+' assertions')"</automated>
  </verify>
  <done>The suite proves both SEG-10 branches against the real database, proves the high-risk branch performs no side effect, proves the scope cross-check rejects a cross-client tool call, and proves the approval queue is invisible to client contacts. It is NOT run here — plan 04-12 runs it live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification run → real WhatsApp API | A careless suite could deliver real messages to real phones |
| Verification run → real database | The suite writes to the same tables production uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-39 | Tampering | Real outbound sends during a test run | mitigate | `globalThis.fetch` is stubbed before the module under test is exercised and restored in `finally`, following `verify-whatsapp-send.ts`'s existing pattern; the assertion set counts stub invocations rather than trusting absence. |
| T-04-40 | Tampering | Test rows polluting a real agency | mitigate | Everything is seeded under a randomized `verify-agent-*` agency id and removed in `finally` via FK cascade — the same convention every prior `db:verify-*` script uses, and 02-07 verified leaves zero residue. |
| T-04-41 | Information Disclosure | A false-green suite masking an RLS regression | mitigate | Assertions target exact row counts (`0`, `1`, `2`) rather than "not an error", and the two most important ones (21, 15) assert absence explicitly rather than inferring it. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run lint` exits 0
- The script is NOT executed in this plan (no live database assumed) — plan 04-12 executes it
</verification>

<success_criteria>
- `scripts/verify-agent.ts` exists with 23+ assertions across two sections
- It calls the real `buildAgentContext`, `classifyAndExecute` and `toolsFor`, never reimplementations
- Outbound sends are stubbed and counted
- Seeded data is cleaned up in a `finally`
</success_criteria>

<output>
After completion, create `.planning/phases/04-agente-conversacional-core/04-08-SUMMARY.md`
</output>
