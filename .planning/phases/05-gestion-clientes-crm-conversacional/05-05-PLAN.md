---
phase: 05-gestion-clientes-crm-conversacional
plan: 05
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - messages/es.json
  - messages/en.json
  - app/[locale]/dashboard/layout.tsx
  - app/[locale]/dashboard/clients/page.tsx
  - app/[locale]/dashboard/clients/client-list.tsx
  - app/[locale]/dashboard/clients/client-search.tsx
autonomous: true
requirements: [CLI-04, CLI-05]

must_haves:
  truths:
    - "A signed-in team member can reach /dashboard/clients from the main nav"
    - "The list page shows only the clients RLS already lets that member see, with zero manual filtering in the page"
    - "Typing in the search box narrows the list by name/industry/notes without a full page reload or a submit button"
    - "An agency with zero clients, and a search with zero matches, each show a distinct, translated empty state"
  artifacts:
    - path: "app/[locale]/dashboard/clients/page.tsx"
      provides: "list + search Server Component, reads the ?q= search param, calls listClients(q)"
      exports: ["default"]
    - path: "messages/es.json"
      provides: "the complete Clients i18n namespace this whole phase's UI depends on (list/form/ficha/errors/industries)"
  key_links:
    - from: "app/[locale]/dashboard/clients/client-search.tsx"
      to: "app/[locale]/dashboard/clients/page.tsx"
      via: "debounced ?q= URL param sync, read server-side via searchParams"
      pattern: "useSearchParams|searchParams"
    - from: "app/[locale]/dashboard/clients/page.tsx"
      to: "lib/clients/list-clients.ts"
      via: "listClients(q) — no manual role/agency filter added in the page"
      pattern: "listClients\\("
---

<objective>
Build the CLI-04 web surface (D-11's reinterpretation: same access the
member already has by role, filtered by free text) and, in the same plan,
write the COMPLETE `Clients` i18n namespace for the whole phase — list,
search, empty states, form labels/errors, industry labels, notes caption,
assignment labels — so every later UI plan (create/edit form, ficha) only
ever CONSUMES `t()` calls against keys that already exist here. This
avoids every later plan re-touching `messages/{es,en}.json` and creating
file-ownership conflicts across parallel plans.

Purpose: this is the "search by name or topic" reinterpretation D-11 marks
as this phase's central decision — the search box is not a special
feature, it is one text filter over the exact same RLS-scoped rows the
member already sees.
Output: `/dashboard/clients` (list + search), a nav link to it, and the
full `Clients` namespace in both locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-CONTEXT.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-UI-SPEC.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-PATTERNS.md

<interfaces>
`listClients(query?: string): Promise<ClientRow[]>` — `lib/clients/list-clients.ts`
(plan 05-03), already handles blank/absent query as "no filter". `ClientRow
= typeof clients.$inferSelect` (id, agencyId, name, phone, email, industry,
notes, createdAt).

`getCurrentTeamMember(): Promise<{id, role, status} | null>` —
`lib/team/current-member.ts`, the same provisioning-guard pattern
`bitacora/page.tsx` and `team/page.tsx` already use (render a "provisioning"
message instead of the page when `null`).

`i18n/navigation.ts` exports `Link`, `redirect`, `usePathname`, `useRouter`,
`getPathname` (locale-aware, built on `i18n/routing.ts`). `useSearchParams`
is NOT exported from there — import it from `next/navigation` directly in
`client-search.tsx` (search params are not locale-prefixed, no wrapper
needed).

`app/[locale]/dashboard/layout.tsx`'s current nav (read the full file, 47
lines): a flat row of `<Link>`s reading from the `Dashboard.nav.*` i18n
keys — `nav.chat`, `nav.bitacora`, `nav.branding`, `nav.team`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The complete Clients i18n namespace + Dashboard nav key</name>
  <read_first>
    - messages/es.json and messages/en.json (the full files — Team/Bitacora/BrandSettings namespace shapes to mirror exactly, and the Dashboard.nav object to extend)
    - .planning/phases/05-gestion-clientes-crm-conversacional/05-UI-SPEC.md, "Copywriting Contract" and "Industry Dropdown (D-04)" tables (every es/en pair this task needs is already written there verbatim)
  </read_first>
  <files>messages/es.json, messages/en.json</files>
  <action>
Add one new top-level `Clients` namespace object to BOTH files, and one new
key `nav.clients` inside the existing `Dashboard.nav` object in both files
(`"Clientes"` / `"Clients"`).

`Clients` namespace keys (es values from the UI-SPEC Copywriting Contract
table verbatim where the table gives one; en values are your own faithful
translation of the same table's en column; for the handful of keys the
table does not give literal copy for — `subtitle`, the 5 field
labels/placeholders, `pagosHeading`, `appointmentsHeading` — write short,
direct, sentence-case copy consistent with `Team`/`Bitacora`'s existing
tone, no filler, no ALL-CAPS):
`title` ("Clientes"/"Clients"), `subtitle` (your own one-liner, e.g. "Todo
lo que tu equipo sabe de cada cliente, en un solo lugar."), `newClient`
("Nuevo cliente"/"New client"), `searchPlaceholder` (from the table),
`emptyNoClients` (object: `title`, `subtitle`, both from the table's "no
clients at all" row), `emptyNoResults` (object: `title`, `subtitle`, both
from the table's "no matches" row, `{query}` placeholder preserved
literally), `emptyPayments` (from the table), `emptyAppointments` (from the
table), `createSubmit` + `createSubmitting` (from the table's create-submit
row, split into idle/pending strings), `editSubmit` + `editSubmitting`
(same, edit-submit row), `nameLabel`/`namePlaceholder`,
`phoneLabel`/`phonePlaceholder`, `emailLabel`/`emailPlaceholder`,
`industryLabel`, `notesLabel`/`notesPlaceholder` (your own short labels —
"Nombre"/"Name", "Teléfono"/"Phone", "Correo"/"Email", "Industria"/
"Industry", "Notas"/"Notes", each with a minimal placeholder), `industries`
(object with exactly the 8 codes plus a 9th key `notSpecified`, all from
the UI-SPEC's Industry Dropdown table, es/en columns), `notesTeamOnlyCaption`
(from the table), `errors` (object: `nameRequired`, `saveFailed`, both from
the table), `assignmentLabel` (from the table), `reassign` (from the
table), `unassigned` (from the table), `editLink` ("Editar"/"Edit"),
`pagosHeading` ("Pagos"/"Payments"), `appointmentsHeading` ("Próximas
citas"/"Upcoming appointments"). Do NOT add a `duplicateNameConfirm`
key — that string is hardcoded directly inside the agent tool
(`lib/agent/tools/create-client.ts`, plan 05-04 already shipped it), never
routed through next-intl, matching every other agent-relayed string in this
codebase (e.g. `deliver-to-client.ts`'s own Spanish literals).

Reuse, do not duplicate: the ficha's conversation-history section (built in
plan 05-07) will call `useTranslations("Bitacora")` directly for
`conversationHeading`/`conversationEmpty`/`direction.*` — genuine
cross-namespace reuse, not a copied value. Do not add those 3 keys under
`Clients` in this task.
  </action>
  <acceptance_criteria>
    - both files remain valid JSON (`node -e "JSON.parse(require('fs').readFileSync('messages/es.json','utf8'))"` and the same for en.json exit 0)
    - `grep -q '"Clients"' messages/es.json` exits 0 and `grep -q '"Clients"' messages/en.json` exits 0
    - `grep -c "restaurants_food\|health_beauty\|fashion_retail\|professional_services\|real_estate\|fitness_sports\|education\|notSpecified" messages/es.json` reports 8 for both files
    - `grep -q '"clients":' messages/es.json` exits 0 (the new Dashboard.nav.clients key)
    - `grep -q "duplicateNameConfirm" messages/es.json` reports no match (deliberately not added here)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const es=require('./messages/es.json');const en=require('./messages/en.json');if(!es.Clients||!en.Clients)throw new Error('Clients namespace missing');const need=['title','subtitle','newClient','searchPlaceholder','emptyNoClients','emptyNoResults','emptyPayments','emptyAppointments','createSubmit','editSubmit','nameLabel','phoneLabel','emailLabel','industryLabel','notesLabel','industries','notesTeamOnlyCaption','errors','assignmentLabel','reassign','unassigned','editLink','pagosHeading','appointmentsHeading'];for(const k of need){if(!(k in es.Clients))throw new Error('es.Clients missing '+k);if(!(k in en.Clients))throw new Error('en.Clients missing '+k);}const codes=['restaurants_food','health_beauty','fashion_retail','professional_services','real_estate','fitness_sports','education','other','notSpecified'];for(const c of codes){if(!(c in es.Clients.industries))throw new Error('missing industry '+c);}if(!es.Dashboard.nav.clients||!en.Dashboard.nav.clients)throw new Error('nav.clients missing');console.log('ok')"</automated>
  </verify>
  <done>Both locale files have a complete Clients namespace covering every string this phase's UI needs, plus a Dashboard.nav.clients key; no later plan needs to touch messages/*.json again.</done>
</task>

<task type="auto">
  <name>Task 2: List page + nav link (Server Components)</name>
  <read_first>
    - app/[locale]/dashboard/bitacora/page.tsx (the provisioning-guard + Promise.all data-fetch pattern to copy)
    - app/[locale]/dashboard/bitacora/audit-list.tsx (the plain async Server Component <ul> pattern for client-list.tsx)
    - app/[locale]/dashboard/layout.tsx (the nav row to extend)
    - app/[locale]/dashboard/team/[memberId]/page.tsx (the `params: Promise<{locale, memberId}>` await convention — this page needs `searchParams: Promise<{q?: string}>` instead)
    - lib/clients/list-clients.ts (listClients(query?) signature, plan 05-03)
  </read_first>
  <files>app/[locale]/dashboard/clients/page.tsx, app/[locale]/dashboard/clients/client-list.tsx, app/[locale]/dashboard/layout.tsx</files>
  <action>
Add a `<Link href="/dashboard/clients">{t("nav.clients")}</Link>` to
`app/[locale]/dashboard/layout.tsx`'s nav row, same `hover:underline`
classes as the 4 existing links, positioned after `nav.team` (natural
reading order: chat, bitacora, branding, team, clients).

Create `app/[locale]/dashboard/clients/page.tsx`: `export default async
function ClientsPage({ searchParams }: { searchParams: Promise<{ q?:
string }> })`. Await `searchParams` and `getCurrentTeamMember()` (provision
guard — mirror `bitacora/page.tsx`'s exact `if (!currentMember) { ... }`
block, reusing `Team.provisioning`). Call `listClients(q)` where `q =
resolvedSearchParams.q?.trim() || undefined`. Render: page header (`h1`
"Display" role from `Clients.title`, `Clients.subtitle` beneath it), the
"Nuevo cliente" button (accent-filled per UI-SPEC — `rounded-md bg-zinc-900
px-4 py-2 text-sm font-medium text-white dark:bg-zinc-100 dark:text-zinc-900`,
same classes as `BrandForm`'s submit button) linking to
`/dashboard/clients/new`, the `<ClientSearch initialQuery={q ?? ""} />`
client component, then either `Clients.emptyNoClients` (when the agency has
zero clients AND no query is active) or `Clients.emptyNoResults` (when a
query is active and returned zero rows, interpolating `{query}`) or
`<ClientList clients={clients} />`.

Create `app/[locale]/dashboard/clients/client-list.tsx`: plain async Server
Component, same `<ul className="flex flex-col divide-y
divide-zinc-200 dark:divide-zinc-800">` shape as `AuditList`, one `<li>` per
client linking to `/dashboard/clients/[id]` (use the locale-aware `Link`
from `@/i18n/navigation`), showing name (Body role) and, if present, an
industry pill (`rounded-full px-2 py-0.5 text-xs font-medium bg-zinc-100
text-zinc-700 dark:bg-zinc-800 dark:text-zinc-300`, reading its label via
`t(\`industries.${client.industry}\`)`).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -q "nav.clients" app/[locale]/dashboard/layout.tsx` exits 0
    - `grep -q "listClients(" app/[locale]/dashboard/clients/page.tsx` exits 0
    - `grep -q "getCurrentTeamMember" app/[locale]/dashboard/clients/page.tsx` exits 0
    - `grep -c "WHERE\|where(" app/[locale]/dashboard/clients/page.tsx app/[locale]/dashboard/clients/client-list.tsx` reports 0 (no manual filtering added in either component — RLS + listClients' own filter are the only scoping)
    - `grep -q "emptyNoClients\|emptyNoResults" app/[locale]/dashboard/clients/page.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>/dashboard/clients renders the RLS-scoped, optionally-filtered client list with correct empty states; the nav links to it.</done>
</task>

<task type="auto">
  <name>Task 3: Debounced search box (Client Component)</name>
  <read_first>
    - app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx (the useRouter import convention from @/i18n/navigation, for consistency)
    - .planning/phases/05-gestion-clientes-crm-conversacional/05-RESEARCH.md, "Don't Hand-Roll" table (debounce guidance: a small local useEffect+setTimeout, no debounce package — this repo has none)
  </read_first>
  <files>app/[locale]/dashboard/clients/client-search.tsx</files>
  <action>
Create `app/[locale]/dashboard/clients/client-search.tsx`, `"use client"`.
Props: `{ initialQuery: string }`. Local state `value` initialized from
`initialQuery`. Import `usePathname`, `useRouter` from `@/i18n/navigation`
and `useSearchParams` from `next/navigation`. On every `value` change,
debounce ~300ms via a `useEffect` + `setTimeout`/`clearTimeout` cleanup,
then build the target URL: clone `useSearchParams()`'s current params, set
or delete `q` (delete when the trimmed value is empty, so the URL stays
clean at `/dashboard/clients` with no trailing `?q=`), and call
`router.replace(\`${pathname}?${params.toString()}\`, { scroll: false })`
(no full navigation, no page jump). A single `<input type="text">` with
`placeholder={t("searchPlaceholder")}`, no submit button (per UI-SPEC's
"light-JS bias" note), same input classes as `BrandForm`'s text input
(`rounded-md border border-zinc-300 px-3 py-2 text-sm dark:border-zinc-700
dark:bg-zinc-900`), full width on this page (`w-full` or `max-w-md`,
matching the page's own layout rhythm).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -q "use client" app/[locale]/dashboard/clients/client-search.tsx` exits 0
    - `grep -q "setTimeout" app/[locale]/dashboard/clients/client-search.tsx` exits 0
    - `grep -q "router.replace" app/[locale]/dashboard/clients/client-search.tsx` exits 0
    - `grep -q "lodash\|use-debounce" app/[locale]/dashboard/clients/client-search.tsx` reports no match
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Typing in the search box updates the URL's ?q= param (debounced, no full reload), which page.tsx reads server-side to re-filter listClients.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ?q= URL search param → listClients filter | User-controlled input, passed as a plain ILIKE pattern — must not be interpreted as SQL (drizzle's parameterized query already prevents this; no raw string concatenation is introduced here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Tampering (SQL injection via search text) | `?q=` -> `listClients(q)` -> `ilike` | accept (already mitigated upstream) | `listClients` (plan 05-03) builds its `ilike` pattern through drizzle's parameterized query builder, never raw SQL string interpolation — this plan just passes the trimmed string through, introducing no new risk. |
| T-05-16 | Information Disclosure | The list page rendering a client the caller cannot see | mitigate | No manual filter exists in `page.tsx`/`client-list.tsx` — `clients_select_by_role` is the only scope, verified by the "no WHERE/where(" grep gate in Task 2. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx eslint .` exits 0
- Both locale JSON files parse and contain the full Clients namespace
- No live-database assertion here — this is a read-only web surface over already-verified RLS
</verification>

<success_criteria>
- /dashboard/clients is reachable from the nav and lists only RLS-visible clients
- The search box narrows the list via the same listClients(query) filter the agent tool also uses (plan 05-04) — no divergent filter logic anywhere in the phase
- Both locale files carry the complete Clients namespace this whole phase's UI depends on
</success_criteria>

<output>
After completion, create `.planning/phases/05-gestion-clientes-crm-conversacional/05-05-SUMMARY.md`
</output>
