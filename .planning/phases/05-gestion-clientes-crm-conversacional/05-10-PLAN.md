---
phase: 05-gestion-clientes-crm-conversacional
plan: 10
type: execute
wave: 6
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06", "05-07", "05-08", "05-09"]
files_modified:
  - .planning/phases/05-gestion-clientes-crm-conversacional/05-10-SUMMARY.md
autonomous: false
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, SEG-08]

user_setup:
  - service: neon-postgres
    why: "Migrations 0020-0022 must be applied to the real database before any RLS/interceptor assertion means anything"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> genzia -> Connection Details -> pooled connection string (owner role, needs DDL rights, not app_user)"

must_haves:
  truths:
    - "Migrations 0020-0022 are applied to the real Neon database"
    - "The RLS write-policy split is proven against real Postgres, not just in a SQL file"
    - "The interceptor's conditional clientId fix is proven against real Postgres"
    - "No Phase 1-4 verification suite regressed"
    - "A human dictated a new client to the agent and it landed correctly, including the ask-back and duplicate-name cases"
    - "A human confirmed the ficha's visual layout matches the UI-SPEC (notes box, placeholders, assignment control)"
    - "A human confirmed the old Team-page quick-add flow is really gone and the new one works instead"
  artifacts:
    - path: ".planning/phases/05-gestion-clientes-crm-conversacional/05-10-SUMMARY.md"
      provides: "The live evidence for the phase"
  key_links:
    - from: "npm run db:migrate"
      to: "drizzle/migrations/0022_agent_action_catalog_client.sql"
      via: "journal-driven migrator against real Neon"
      pattern: "0022_agent_action_catalog_client"
    - from: "npm run db:verify-clients-crm"
      to: "lib/agent/risk-interceptor.ts"
      via: "exercises the real interceptor and the real clients_insert_by_team_member/clients_update_by_role policies"
      pattern: "classifyAndExecute"
---

<objective>
Prove the phase against reality. Every plan before this one typechecks,
lints, and (for the UI plans) renders — none of that proves the RLS
write-policy split works, the interceptor's conditional clientId fix
works, or that a real human can actually dictate a client into existence.
This is the phase gate.

Purpose: the two research-flagged blockers (RLS write gap, interceptor
clientId gap) are only actually closed once Postgres and a live agent turn
say so.
Output: 3 migrations applied, 4 suites green with real counts recorded,
and three human confirmations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-RESEARCH.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-UI-SPEC.md
@.planning/phases/04-agente-conversacional-core/04-12-SUMMARY.md

<known_open_gaps>
Carried forward, NOT introduced by this phase — do not treat any of these
as a failure of this plan:

1. **Meta App Review (from 03-08).** Still open; irrelevant here since this
   phase's dictation checkpoint uses the web chat, same as 04-12 did.

2. **SEG-12 (from 02-07).** `db:verify-identity` is expected to report
   16/18 with the same two known failures. A third failure is a
   regression.

3. **Credential rotation (from 03-08).** Still pending, still out of scope.
</known_open_gaps>

<network_check>
Before treating any step as impossible, verify egress directly in THIS
session: `psql "$DATABASE_URL" -c 'select 1'`. Sandboxing has varied
session to session in this project — do not assume the block without
checking.
</network_check>

<known_migration_risk>
Plan 05-01's own migration comment already flags this: migration 0020's
`clients_contact_required_check` constraint will fail to apply if any
existing real `clients` row has both `phone` and `email` null — the exact
shape the OLD `addClient(name)` quick-add flow (removed in plan 05-09)
produced. If `npm run db:migrate` fails on this constraint, that is a REAL
finding, not an environment problem: query `SELECT id, name FROM clients
WHERE phone IS NULL AND email IS NULL`, and if any rows come back, either
backfill them (`UPDATE clients SET phone = '(sin dato)' WHERE phone IS
NULL AND email IS NULL`) with the team's/user's knowledge, or ask the user
how they want those specific rows handled — do not weaken the constraint
to make the migration pass silently.
</known_migration_risk>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migrations 0020-0022 and run the full suite matrix</name>
  <read_first>
    - scripts/migrate.ts (what npm run db:migrate does and which driver it uses)
    - .planning/phases/03-integraci-n-con-whatsapp-meta/03-08-SUMMARY.md (the hand-application fallback procedure, if npm run db:migrate is blocked by the environment's permission classifier)
    - drizzle/migrations/meta/_journal.json (entries 20-22 must be present and in order)
    - the `<known_migration_risk>` block above
  </read_first>
  <files>(no source changes expected; any real fix found here is a deviation to document in the SUMMARY)</files>
  <action>
1. Verify egress (see `<network_check>`). Record the result either way.
2. `npm run db:migrate`. Expect exactly three new migrations applied:
   `0020_clients_crm_fields`, `0021_clients_write_by_team_member`,
   `0022_agent_action_catalog_client`. If migration 0020 fails on
   `clients_contact_required_check`, follow `<known_migration_risk>` — do
   not weaken the constraint. If the environment blocks `npm run
   db:migrate` outright, fall back to 03-08's hand-application procedure
   and say so in the SUMMARY.
3. Run, in this order, recording exact pass counts:
   - `npm run db:verify-rls` — expect 7/7 (Phase 1 regression)
   - `npm run db:verify-identity` — expect 16/18 with exactly the two
     known SEG-12 failures (`8b`/`8c`). A third failure is a regression:
     stop and investigate before continuing.
   - `npm run db:verify-whatsapp` — expect 28/28 (Phase 3 regression)
   - `npm run db:verify-agent-rls` — expect fully green (Phase 4
     regression — this phase's new RLS policies must not have broken
     `audit_log`/`messages` scoping)
   - `npm run db:verify-agent` — expect fully green (Phase 4 SEG-10 engine
     regression — the 4 new catalog rows and the interceptor change must
     not have broken the 2 pre-existing tools)
   - `npm run db:verify-clients-crm` — expect fully green, ZERO failures.
     This is the phase's own gate: the RLS write split, the merge-based
     partial update, the interceptor's conditional clientId fix, D-07,
     D-09, and the 4 catalog rows all get proven here for real.
4. If any suite fails, apply Rule 1: fix the real bug, re-run the whole
   matrix, and document the finding in the SUMMARY. Do not weaken an
   assertion to make it pass.
5. Confirm no residue: `select count(*) from agencies where id like
   'verify-%'` returns 0 after the runs.
  </action>
  <acceptance_criteria>
    - `npm run db:verify-clients-crm` exits 0 with zero [FAIL] lines
    - `npm run db:verify-agent-rls` exits 0 with zero [FAIL] lines
    - `npm run db:verify-agent` exits 0 with zero [FAIL] lines
    - `npm run db:verify-rls` reports 7/7
    - `npm run db:verify-whatsapp` reports 28/28
    - `npm run db:verify-identity` reports 16/18 with failures 8b and 8c and no others
    - `select count(*) from agencies where id like 'verify-%'` returns 0
    - The SUMMARY records the exact numbers, not "all green"
  </acceptance_criteria>
  <verify>
    <automated>npm run db:verify-clients-crm && npm run db:verify-agent-rls && npm run db:verify-agent && npm run db:verify-rls</automated>
  </verify>
  <done>Three migrations are live on Neon; the phase's own new suite passes fully; no earlier phase regressed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Dictate a client to the agent — create, ask-back, duplicate-name</name>
  <read_first>
    - app/[locale]/dashboard/chat/page.tsx (where the surface lives)
    - lib/agent/tools/create-client.ts (the exact D-07/D-09 strings to expect verbatim)
    - .planning/phases/05-gestion-clientes-crm-conversacional/05-08-SUMMARY.md (anything the executor deviated on)
  </read_first>
  <what-built>
    Four new agent tools (create_client, update_client, list_clients,
    get_client), all risk `low`, wired through the same `classifyAndExecute`
    gate as the 2 pre-existing tools. Any team member (not just admin) can
    now create/edit/find/inspect clients purely by talking to the agent.
  </what-built>
  <how-to-verify>
    Start the app (`npm run dev`) with `DATABASE_URL` and
    `OPENROUTER_API_KEY` set. Sign in as a NON-admin team member (a
    "member"-role account with no prior client assignments) and go to
    `/dashboard/chat`.

    1. **Ask-back (D-07).** Say "Dame de alta un cliente nuevo llamado
       Panadería Sol" (name only, no phone/email). The agent must ask for a
       phone or email before creating anything — it must NOT claim a
       client was created. Then provide a phone number in the next
       message; the agent should now actually create it.
    2. **Self-assignment (D-13).** Go to `/dashboard/clients` as this same
       member — "Panadería Sol" must appear in the list (a member can see
       a client they themselves created, even with zero admin-granted
       assignments).
    3. **Duplicate-name (D-09).** Say "Dame de alta otro cliente llamado
       Panadería Sol, teléfono +573001112233". The agent's reply must ask
       whether this is the same business or a new one — it must NOT
       silently create a second identical client without asking, and it
       must NOT refuse outright either (D-09 is a soft confirmation, not a
       hard block).
    4. **Dictated edit (D-10).** Say "Actualiza las notas de Panadería Sol:
       cliente nuevo, contacto preferido por la mañana." Then open its
       ficha (`/dashboard/clients/[id]`) and confirm the notes field shows
       exactly that text, and that `phone`/`industry` are unchanged from
       step 1 (proves the merge-not-replace behavior, not just that SOME
       write happened).
    5. **Search (D-11/D-12).** Say "Buscá clientes de panadería" (or any
       text matching what you entered as industry/notes) — the agent's
       reply must list only clients this member can actually see.

    Expected: four confirmations, all matching the exact behaviors above.
  </how-to-verify>
  <acceptance_criteria>
    - The agent never claims a client was created when contact info was missing
    - The member sees the client they created themselves in /dashboard/clients without any admin action
    - The duplicate-name message asks a question, neither silently duplicates nor hard-blocks
    - The dictated edit changes only the mentioned field, confirmed by re-reading the ficha
    - The agent's search reply lists only clients visible to this member
  </acceptance_criteria>
  <resume-signal>Type "approved" or paste the exact agent replies that failed.</resume-signal>
  <done>A human confirmed the full dictation path — create with ask-back, self-assignment, duplicate-name confirmation, partial edit, and search — all against a real LLM turn and real Postgres.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual check of the ficha + confirm the old Team quick-add is gone</name>
  <read_first>
    - .planning/phases/05-gestion-clientes-crm-conversacional/05-UI-SPEC.md, "Ficha Layout" and "Field Visibility Layering" sections
    - .planning/phases/05-gestion-clientes-crm-conversacional/05-VALIDATION.md, "Manual-Only Verifications" table
  </read_first>
  <what-built>
    The client ficha at `/dashboard/clients/[clientId]`: header + industry
    pill, contact info, assignment chips with an admin-only inline
    reassign control, a visually distinct team-only notes box (lock icon +
    caption), two placeholder sections for pagos/próximas citas, and
    real, read-only conversation history scoped to that one client. The
    Team page's old name-only quick-add form is gone, replaced by a link
    to `/dashboard/clients/new`.
  </what-built>
  <how-to-verify>
    1. As an admin, open a client's ficha. Confirm: the notes box has a
       lock icon, the caption "Solo el equipo puede ver esto." directly
       under the notes label, and its own bordered box distinct from every
       other field. Confirm the pagos/próximas citas sections render as
       empty-state boxes with the expected copy, not blank space and not a
       broken component.
    2. Still as admin, click "Reasignar" on the assignment panel — confirm
       it expands inline (no navigation, no modal) into a checkbox list of
       every team member, and toggling one immediately updates the chips
       above it.
    3. Sign in as a non-admin member assigned to that same client. Open
       its ficha — confirm the assignment chips render but NO "Reasignar"
       control appears at all (not even disabled).
    4. Go to `/dashboard/team/[any member]`. Confirm the old "Nuevo
       cliente" quick-add text input + submit button no longer exists;
       confirm a link to `/dashboard/clients/new` is in its place and it
       actually navigates there.
    5. From `/dashboard/clients`, confirm the search box narrows the list
       as you type (no full page reload, no submit button) and that the
       empty state differs between "no clients at all" and "no results for
       this search."

    Expected: five confirmations, all matching 05-UI-SPEC.md exactly.
  </how-to-verify>
  <acceptance_criteria>
    - The notes box is visually distinct from every visible-client field
    - Pagos/próximas citas render as populated-weight empty-state boxes
    - The reassign control is admin-only, expands inline, and updates chips immediately
    - The old Team-page quick-add UI is gone; the new Link works
    - The search box narrows results live, with two distinct empty states
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe exactly which step looked different from 05-UI-SPEC.md.</resume-signal>
  <done>A human confirmed the ficha's visual layering and the assignment control's admin-only gating, and confirmed D-18's cleanup actually shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification run → production data | These commands run against the real Neon database |
| Live agent run → real client data | The dictation checkpoint creates real rows in the signed-in user's real agency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-25 | Tampering | Verification data left in the real database | mitigate | `db:verify-clients-crm` seeds under a randomized `verify-*` agency and cleans up in a `finally`; Task 1 asserts a zero count afterwards. |
| T-05-26 | Repudiation | A suite reported as "green" without numbers | mitigate | Acceptance criteria require exact counts (7/7, 16/18 with 8b/8c, 28/28) in the SUMMARY. |
| T-05-27 | Tampering | A migration silently loosened to work around the contact-required constraint | mitigate | `<known_migration_risk>` explicitly forbids weakening the constraint; any real conflicting row must be surfaced to the user, not silently papered over. |
</threat_model>

<verification>
- Three migrations applied to real Neon, journal and `__drizzle_migrations` consistent
- 6 suites run with exact expected counts recorded (rls, identity, whatsapp, agent-rls, agent, clients-crm)
- Two human confirmations obtained
- Any bug found is fixed and the whole matrix re-run, per Rule 1
</verification>

<success_criteria>
- db:verify-clients-crm and db:verify-agent-rls/db:verify-agent are fully green against real Postgres
- No Phase 1-4 regression beyond the two documented SEG-12 failures
- A human confirmed the dictation path end to end, including ask-back and duplicate-name
- A human confirmed the ficha's visual layering and D-18's cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/05-gestion-clientes-crm-conversacional/05-10-SUMMARY.md`
</output>
