---
phase: 05-gestion-clientes-crm-conversacional
plan: 07
type: execute
wave: 4
depends_on: ["05-03", "05-05"]
files_modified:
  - app/[locale]/dashboard/clients/[clientId]/page.tsx
  - app/[locale]/dashboard/clients/[clientId]/conversation-history.tsx
  - app/[locale]/dashboard/clients/[clientId]/assignment-panel.tsx
autonomous: true
requirements: [CLI-02, CLI-05, SEG-08]

must_haves:
  truths:
    - "A client's ficha shows its real conversation history, scoped to that one client only"
    - "The ficha shows empty-state placeholders for pagos and próximas citas, same visual weight as real content"
    - "The notes field is visually marked team-only (lock icon + caption + its own box), unlike every other field"
    - "The ficha shows which team members are assigned; only an admin sees a control to change it"
    - "A team member who cannot see a given client is redirected away from its ficha, never shown a blank/broken page"
  artifacts:
    - path: "app/[locale]/dashboard/clients/[clientId]/page.tsx"
      provides: "the ficha — composes header, contact info, assignment, notes, placeholders, conversation history"
      exports: ["default"]
    - path: "app/[locale]/dashboard/clients/[clientId]/conversation-history.tsx"
      provides: "read-only conversation playback for one client, reusing Bitacora's own i18n strings and chat-panel.tsx's bubble classes"
    - path: "app/[locale]/dashboard/clients/[clientId]/assignment-panel.tsx"
      provides: "assignment chips + admin-only inline reassign toggle, reusing assignClient/unassignClient verbatim"
  key_links:
    - from: "app/[locale]/dashboard/clients/[clientId]/conversation-history.tsx"
      to: "lib/audit/list-audit-log.ts"
      via: "listConversationMessagesForClient(clientId) — no clientId filter added anywhere else on the page"
      pattern: "listConversationMessagesForClient\\("
    - from: "app/[locale]/dashboard/clients/[clientId]/assignment-panel.tsx"
      to: "lib/clients/assign-client.ts"
      via: "assignClient/unassignClient, unchanged, same optimistic-toggle pattern as assign-clients-form.tsx"
      pattern: "assignClient\\(|unassignClient\\("
---

<objective>
Build the ficha (CLI-02's "consulta de ficha: historial, pagos, próximas
citas", CLI-05/SEG-08's visible-cliente/solo-equipo layering, D-15's real
conversation history + placeholder sections, D-16's assignment display +
admin-only reassign control) exactly per 05-UI-SPEC.md's "Ficha Layout"
section — every visual decision (spacing, colors, the notes lock icon, the
placeholder boxes) is already locked there; this plan implements it, it
does not re-derive it.

Purpose: this is the other half of CLI-02 (the ficha for the client just
created) and the phase's clearest expression of SEG-08 (notes visibly
marked team-only, nothing else singled out).
Output: `/dashboard/clients/[clientId]`, composed from two new
sub-components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-CONTEXT.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-UI-SPEC.md

<interfaces>
`getClient(clientId: string): Promise<ClientRow | null>` —
`lib/clients/get-client.ts` (plan 05-03). `ClientRow` includes `phone`,
`email`, `industry`, `notes`, all `string | null`.

`listConversationMessagesForClient(clientId: string, limit?: number):
Promise<ConversationEntry[]>` — `lib/audit/list-audit-log.ts` (plan 05-03).
`ConversationEntry = { id, createdAt, direction, channel, messageType,
textBody, clientName }`.

`listAssignedTeamMemberIds(clientId: string): Promise<string[]>` and
`listTeamMembers(): Promise<TeamMemberRow[]>` — `lib/clients/list-clients.ts`
(plan 05-03) and `lib/team/list-members.ts` (existing). `TeamMemberRow`
includes `id`, `email`, `role`.

`assignClient(clientId, teamMemberId): Promise<{ok:true} | {ok:false;
error:"not_admin"}>` and `unassignClient(clientId, teamMemberId): Promise<...>`
— `lib/clients/assign-client.ts`, UNCHANGED signatures (only their internal
insert body moved to `insertAssignment` in plan 05-03; the exported
functions still self-check admin server-side).

`getCurrentTeamMember(): Promise<{id, role, status} | null>` —
`lib/team/current-member.ts`.

`Bitacora` i18n namespace already has `conversationHeading`,
`conversationEmpty`, `direction.inbound`/`direction.outbound`, `team` —
`conversation-history.tsx` reads THESE directly via
`getTranslations("Bitacora")`, genuine cross-namespace reuse, not a copy
(05-05 deliberately did not duplicate these under `Clients`).

`chat-panel.tsx`'s bubble classes to reuse for read-only playback:
`rounded-2xl px-4 py-2.5 text-sm shadow-sm`, `bg-zinc-900 text-white
dark:bg-zinc-100 dark:text-zinc-900` for one side, `bg-white text-zinc-900
dark:bg-zinc-800 dark:text-zinc-100` for the other; container `rounded-3xl
border border-zinc-200 bg-zinc-50/60 p-4 dark:border-zinc-800
dark:bg-zinc-900/40 max-h-[28rem] overflow-y-auto`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: conversation-history.tsx + assignment-panel.tsx</name>
  <read_first>
    - app/[locale]/dashboard/bitacora/page.tsx (the conversation `<ul>` section, lines ~90-113 — the exact block to extract into its own component)
    - app/[locale]/dashboard/chat/chat-panel.tsx (the bubble classes and left/right alignment convention)
    - app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx (the full file — the optimistic-toggle-with-rollback pattern, inverted axis: this component iterates team members for ONE client, not clients for ONE team member)
    - lib/clients/assign-client.ts (assignClient/unassignClient's unchanged signatures, post plan 05-03)
    - .planning/phases/05-gestion-clientes-crm-conversacional/05-UI-SPEC.md, "Ficha Layout" section 3 and 6
  </read_first>
  <files>app/[locale]/dashboard/clients/[clientId]/conversation-history.tsx, app/[locale]/dashboard/clients/[clientId]/assignment-panel.tsx</files>
  <action>
Create `conversation-history.tsx`, an async Server Component. Props: `{
clientId: string }`. Fetch `listConversationMessagesForClient(clientId)`
and `getTranslations("Bitacora")` + `getLocale()`. Render the heading
(`t("conversationHeading")`, Heading role) then either `t("conversationEmpty")`
or the `rounded-3xl` scrollable container with one bubble per message —
right-aligned/dark for `direction === "outbound"`, left-aligned/light for
`"inbound"` (or pick the alignment convention that matches
`chat-panel.tsx`'s `role === "user"` vs `"agent"` mapping — direction is the
ficha's available signal, use it consistently), each bubble's text
truncated the same way `bitacora/page.tsx`'s own `truncate()` helper does
(copy that helper verbatim, 160-char limit) since these are historical
messages, not a live thread. No input box, no attachment button, no send
button — playback only, per UI-SPEC.

Create `assignment-panel.tsx`, `"use client"`. Props: `{ clientId: string;
allMembers: { id: string; email: string }[]; initialAssignedIds: string[];
isAdmin: boolean }`. Render `Clients.assignmentLabel` then a chip per
currently-assigned member (pill classes reused from the industry badge —
`rounded-full bg-zinc-100 px-2 py-0.5 text-xs font-medium text-zinc-700
dark:bg-zinc-800 dark:text-zinc-300`), or `Clients.unassigned` text when
none. When `isAdmin` is true, also render a `Clients.reassign` text-button
that toggles a local `expanded` boolean; when expanded, render the SAME
optimistic-toggle checkbox list `assign-clients-form.tsx` already
implements, but iterating `allMembers` (one row per team member, checkbox
reflecting whether THIS client is assigned to that member) instead of
iterating clients — call `assignClient(clientId, member.id)` /
`unassignClient(clientId, member.id)` exactly as that file does, same
optimistic-set-then-rollback-on-`{ok:false}` logic, same
`Team.errors.notAdmin` error text on rollback (reuse that key via a second
`useTranslations("Team")` call — do not duplicate it under `Clients`). When
`isAdmin` is false, render only the chips, no control at all — not even a
disabled one.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -q "listConversationMessagesForClient" app/[locale]/dashboard/clients/[clientId]/conversation-history.tsx` exits 0
    - `grep -q "getTranslations(\"Bitacora\")" app/[locale]/dashboard/clients/[clientId]/conversation-history.tsx` exits 0
    - `grep -q "Clients.conversationHeading\|conversationHeading\":" app/[locale]/dashboard/clients/[clientId]/conversation-history.tsx` reports no match (must read the key via Bitacora's namespace call, not a duplicated Clients key)
    - `grep -q "use client" app/[locale]/dashboard/clients/[clientId]/assignment-panel.tsx` exits 0
    - `grep -q "assignClient(" app/[locale]/dashboard/clients/[clientId]/assignment-panel.tsx` exits 0 and `grep -q "unassignClient(" app/[locale]/dashboard/clients/[clientId]/assignment-panel.tsx` exits 0
    - `grep -q "isAdmin" app/[locale]/dashboard/clients/[clientId]/assignment-panel.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>conversation-history.tsx plays back exactly one client's messages via genuine Bitacora-namespace reuse; assignment-panel.tsx shows chips to everyone and the reassign control to admins only, using the unmodified assignClient/unassignClient Server Actions.</done>
</task>

<task type="auto">
  <name>Task 2: The ficha page (composition)</name>
  <read_first>
    - app/[locale]/dashboard/bitacora/page.tsx (multi-section SSR composition pattern, Promise.all data-fetch)
    - app/[locale]/dashboard/team/[memberId]/page.tsx (params-await + redirect-on-missing pattern)
    - .planning/phases/05-gestion-clientes-crm-conversacional/05-UI-SPEC.md, "Ficha Layout (CLI-02 / D-15 / D-16)" and "Field Visibility Layering (CLI-05 / SEG-08 / D-06)" sections — the exact box/lock-icon/caption treatment for notes, and the exact placeholder box classes for pagos/próximas citas
  </read_first>
  <files>app/[locale]/dashboard/clients/[clientId]/page.tsx</files>
  <action>
`export default async function ClientFichaPage({ params }: { params:
Promise<{ locale: string; clientId: string }> })`. Await `params`. Fetch,
in parallel: `getClient(clientId)`, `getCurrentTeamMember()`. If
`getClient` returns `null`, `redirect({ href: "/dashboard/clients", locale
})`. If `getCurrentTeamMember()` returns `null`, render the
`Team.provisioning` message (mirror `bitacora/page.tsx`). Otherwise fetch,
in parallel: `listTeamMembers()` and `listAssignedTeamMemberIds(clientId)`.

Render, top to bottom, `flex flex-col gap-6` (per UI-SPEC's Ficha Layout):
1. Header row: client `name` as `h1` (Display role) + industry pill inline
   (reuse the same pill classes as `client-list.tsx`, `t(\`industries.${industry}\`)`,
   only when `industry` is non-null), and an "Editar" link
   (`t("Clients.editLink")`, bordered secondary-button classes —
   `rounded-md border border-zinc-300 px-4 py-2 text-sm font-medium
   dark:border-zinc-700`) to `/dashboard/clients/${clientId}/edit`, aligned
   top-right (`flex items-start justify-between`).
2. Contact info: `phone`/`email` as two label/value pairs (Label role
   above, Body role value; omit a pair entirely when that field is null,
   never render "null" or an empty value), `flex flex-wrap gap-6`.
3. `<AssignmentPanel clientId={clientId} allMembers={teamMembers.map(m =>
   ({id: m.id, email: m.email}))} initialAssignedIds={assignedIds}
   isAdmin={currentMember.role === "admin"} />`.
4. Notes (team-only, per Field Visibility Layering): a `rounded-lg border
   border-zinc-200 bg-zinc-50 p-4 dark:border-zinc-800 dark:bg-zinc-900/40`
   box, containing a `Lock` icon (`lucide-react`, size 12) beside
   `t("Clients.notesLabel")`, then `t("Clients.notesTeamOnlyCaption")` in
   `text-xs text-zinc-500` directly under the label, then the client's
   `notes` value (or nothing rendered at all — not even an empty-state
   sentence — when `notes` is null; an empty notes box for a client with no
   notes yet is a reasonable, UI-SPEC-consistent minimal choice).
5. Two placeholder boxes, side by side or stacked (`flex flex-col gap-4` is
   fine — UI-SPEC does not mandate a specific axis), each `rounded-lg
   border border-zinc-200 p-4 dark:border-zinc-800`: one headed
   `t("Clients.pagosHeading")` with body `t("Clients.emptyPayments")`, one
   headed `t("Clients.appointmentsHeading")` with body
   `t("Clients.emptyAppointments")`. No call-to-action button inside
   either, per UI-SPEC.
6. `<ConversationHistory clientId={clientId} />`.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -q "getClient(" app/[locale]/dashboard/clients/[clientId]/page.tsx` exits 0
    - `grep -q "redirect(" app/[locale]/dashboard/clients/[clientId]/page.tsx` exits 0
    - `grep -q "Lock" app/[locale]/dashboard/clients/[clientId]/page.tsx` exits 0 (lucide-react icon on the notes box)
    - `grep -q "AssignmentPanel" app/[locale]/dashboard/clients/[clientId]/page.tsx` exits 0
    - `grep -q "ConversationHistory" app/[locale]/dashboard/clients/[clientId]/page.tsx` exits 0
    - `grep -c "WHERE\|\\.where(" app/[locale]/dashboard/clients/[clientId]/page.tsx` reports 0 (no manual filtering in the page — every fetch is a plain call to an already-scoped function)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The ficha renders every section from 05-UI-SPEC.md's layout in order, redirects cleanly for a missing/inaccessible client, and the notes box is the one visually distinct team-only element on the page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ficha URL clientId → getClient/listConversationMessagesForClient | Same enumeration concern as the edit route: must not distinguish "doesn't exist" from "not visible to you" |
| Assignment reassign control → assignClient/unassignClient | Must remain admin-gated server-side even though the UI already hides the control from non-admins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | Information Disclosure | A member viewing another agency's/unassigned client's ficha via a guessed clientId | mitigate | `getClient` returns `null` uniformly; the page redirects on `null` before fetching anything else (team members, conversation history) — no partial page ever renders for an inaccessible client. |
| T-05-20 | Elevation of Privilege | A non-admin calling assignClient/unassignClient directly (bypassing the hidden UI control) | mitigate (inherited, unchanged) | Both functions still call `assertCallerIsAdmin` server-side, exactly as before plan 05-03's extraction — the UI's `isAdmin` prop is a rendering convenience, never the boundary. |
| T-05-21 | Information Disclosure | Rendering the client's `notes` (team-only, SEG-08) on any surface a client_contact could reach | accept (out of scope this phase, per RESEARCH.md) | This ficha route sits under `/dashboard`, gated by Clerk + `withTenantContext` (team-only by construction); no client-facing portal exists yet (Phase 9). Confirmed by RESEARCH.md's own reading of `build-context.ts`: no client-facing surface queries `clients` today. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx eslint .` exits 0
- Manual visual check per 05-VALIDATION.md's "Manual-Only Verifications" table happens in plan 05-10, not here
</verification>

<success_criteria>
- The ficha shows real conversation history scoped to one client, assignment chips + admin-only reassign, a visually distinct team-only notes box, and two placeholder sections
- A missing/inaccessible clientId redirects cleanly, never renders a broken page
</success_criteria>

<output>
After completion, create `.planning/phases/05-gestion-clientes-crm-conversacional/05-07-SUMMARY.md`
</output>
