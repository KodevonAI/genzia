---
phase: 05-gestion-clientes-crm-conversacional
plan: 09
type: execute
wave: 5
depends_on: ["05-03", "05-06"]
files_modified:
  - lib/clients/assign-client.ts
  - app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx
autonomous: true
requirements: [CLI-01]

must_haves:
  truths:
    - "There is exactly one way to create a client in the whole app: name + phone-or-email, enforced identically everywhere"
    - "The Team page's old quick-add box is gone; its place is taken by a link to /dashboard/clients/new"
    - "No code path can create a clients row with neither phone nor email after this plan"
  artifacts:
    - path: "app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx"
      provides: "the assignment toggle list, with the old name-only quick-add form replaced by a link to the real create flow"
      exports: ["AssignClientsForm"]
  key_links:
    - from: "app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx"
      to: "app/[locale]/dashboard/clients/new/page.tsx"
      via: "a plain Link, not a Server Action"
      pattern: "href=\"/dashboard/clients/new\""
---

<objective>
Close out D-18: remove the old admin-only, name-only "quick add client"
flow from the Team page (`addClient(name)` + its form in
`assign-clients-form.tsx`) now that `/dashboard/clients/new` is a real,
fully-validated replacement. RESEARCH.md's Pitfall 3 flagged this
explicitly — leaving both paths alive would let a client with neither
phone nor email exist despite D-02, and the phase's own new
`clients_contact_required_check` constraint (plan 05-01) would then reject
this old flow's insert anyway, so it is already silently broken by the
time this plan runs; this plan just removes the dead, misleading UI
instead of leaving it to fail confusingly.

Purpose: D-18, the one explicit "don't let this survive by omission" item
from CONTEXT.md's second discuss-phase pass.
Output: one fewer creation path, one more Link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-CONTEXT.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-RESEARCH.md

<interfaces>
Current `lib/clients/assign-client.ts` exports (post plan 05-03):
`assignClient`, `unassignClient`, `AdminActionResult`, `insertAssignment`
import (from `./insert-assignment`), and the now-to-be-removed `addClient`
+ `AddClientResult`.

Current `assign-clients-form.tsx` (read the full file): imports `addClient`
from `@/lib/clients/assign-client`; local state `newClientName`,
`isAddingClient`; `handleAddClient` calls `addClient(trimmed)` then
`router.refresh()`; JSX renders a `<form>` with a text input + submit
button, using `Team.addClientLabel`/`addClientPlaceholder`/`addClientSubmit`/
`addClientSubmitting` i18n keys.

`Clients.newClient` i18n key already exists (plan 05-05): "Nuevo
cliente"/"New client".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove addClient from assign-client.ts</name>
  <read_first>
    - lib/clients/assign-client.ts (the full current file — addClient's exact body, lines ~88-121, to delete)
  </read_first>
  <files>lib/clients/assign-client.ts</files>
  <action>
Delete the exported `AddClientResult` type and the exported `addClient`
function entirely from `lib/clients/assign-client.ts`. Do not touch
`assignClient`, `unassignClient`, or the `insertAssignment` import —
nothing else in this file changes. If any import of `clients` (the schema)
in this file becomes unused after removing `addClient`'s
`.insert(clients)` call, remove that now-dead import too (check: this file
imports `clients` from `@/lib/db/schema/clients` only for `addClient`'s own
insert — confirm before removing, since `assignClient`/`unassignClient`
never reference the `clients` table directly, only `clientAssignments`).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -q "export.*addClient\|export.*AddClientResult" lib/clients/assign-client.ts` reports no match
    - `grep -q "assignClient\|unassignClient" lib/clients/assign-client.ts` exits 0 (both still present, unchanged)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>addClient/AddClientResult no longer exist; assignClient/unassignClient are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the Team page's quick-add form with a link to /dashboard/clients/new</name>
  <read_first>
    - app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx (the full current file — the exact handler/state/JSX block to remove, lines ~29-31, 73-91, 95-117)
    - app/[locale]/dashboard/team/[memberId]/page.tsx (confirms this component receives no props this change would affect)
  </read_first>
  <files>app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx</files>
  <action>
Remove the `addClient` import, the `newClientName`/`isAddingClient` state,
and the `handleAddClient` function entirely. Remove the `<form
onSubmit={handleAddClient}>` block (the text input + submit button) from
the JSX. In its place, render a plain `<Link href="/dashboard/clients/new">`
(from `@/i18n/navigation`, same import this file already has for
`useRouter`) styled as a bordered secondary button (`rounded-md border
border-zinc-300 px-4 py-2 text-sm font-medium dark:border-zinc-700` — the
same classes the old submit button used, so the visual weight on this page
does not change), reading its label from a SECOND `useTranslations("Clients")`
call (`const tClients = useTranslations("Clients");` alongside the
existing `const t = useTranslations("Team");`) using the already-existing
`Clients.newClient` key — do not add a new i18n key for this. Leave
`Team.addClientLabel`/`addClientPlaceholder`/`addClientSubmit`/
`addClientSubmitting`/`errors.invalidClientName` in `messages/{es,en}.json`
as harmless unused keys (removing them is optional cleanup, not required
by D-18; do not spend a separate task on it).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -q "addClient" app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx` reports no match
    - `grep -q "handleAddClient" app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx` reports no match
    - `grep -q "/dashboard/clients/new" app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx` exits 0
    - `grep -q "useTranslations(\"Clients\")" app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx` exits 0
    - `grep -q "toggle(client.id)" app/[locale]/dashboard/team/[memberId]/assign-clients-form.tsx` exits 0 (the assignment checkbox list itself is untouched)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The Team page's per-member assignment view no longer offers name-only client creation; it links to the real, fully-validated /dashboard/clients/new flow instead. The assignment toggle list itself is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client creation path count | Exactly one validation rule (D-02) must govern every path that can produce a clients row, after this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Tampering (data-integrity drift) | Two divergent client-creation validation rules coexisting | mitigate | This plan removes the only other path; after it, `create-client.ts`/`create-client-action.ts` (plan 05-03) and the `create_client` tool (plan 05-04) are the sole writers of new `clients` rows, both sharing one validation function. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx eslint .` exits 0
- No live-database assertion here — this is a pure code-removal plan
</verification>

<success_criteria>
- addClient/AddClientResult no longer exist anywhere in the codebase
- The Team page's per-member view links to /dashboard/clients/new instead of offering its own creation form
</success_criteria>

<output>
After completion, create `.planning/phases/05-gestion-clientes-crm-conversacional/05-09-SUMMARY.md`
</output>
