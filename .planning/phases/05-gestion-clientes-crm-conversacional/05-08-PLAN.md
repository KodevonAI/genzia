---
phase: 05-gestion-clientes-crm-conversacional
plan: 08
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - scripts/verify-clients-crm.ts
  - package.json
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, SEG-08]

must_haves:
  truths:
    - "A member can INSERT a client and gets auto-assigned; the write is proven against real RLS, not mocked"
    - "A member can UPDATE only a client assigned to them, never one assigned to someone else"
    - "create_client/list_clients classify low and execute without a clientId argument"
    - "update_client/get_client still reject a client_contact targeting a foreign clientId"
    - "D-07 (ask-back) and D-09 (duplicate-name warning) are proven at the tool level, not just read from the code"
  artifacts:
    - path: "scripts/verify-clients-crm.ts"
      provides: "the Nyquist-required integration suite for this phase's RLS split + interceptor fix + agent tools"
      exports: []
  key_links:
    - from: "scripts/verify-clients-crm.ts"
      to: "lib/agent/risk-interceptor.ts"
      via: "classifyAndExecute exercised directly with hand-built toolUse + TurnActor values, same pattern as verify-agent.ts"
      pattern: "classifyAndExecute\\("
    - from: "scripts/verify-clients-crm.ts"
      to: "drizzle/migrations/0021_clients_write_by_team_member.sql"
      via: "member-vs-admin INSERT/UPDATE assertions against the real clients_insert_by_team_member/clients_update_by_role policies"
      pattern: "withResolvedIdentityContext"
---

<objective>
Write the Nyquist-required integration test this phase's Wave-0 gaps
demanded: a real-Neon proof that (a) the RLS write-policy split (plan
05-01) actually lets a member INSERT and lets a member UPDATE only their
own assigned client, and (b) the risk-interceptor fix (plan 05-02) actually
lets `create_client`/`list_clients` through with no `clientId` while
`update_client`/`get_client` still reject a client_contact targeting a
foreign client. This is the single test this phase's RESEARCH.md and
VALIDATION.md both call out as the one that would catch a regression on
either research-flagged blocker.

Purpose: everything through plan 05-04 typechecks without ever touching
real Postgres — a green `tsc` proves nothing about RLS or the interceptor.
This script is what plan 05-10 actually runs against real Neon.
Output: `scripts/verify-clients-crm.ts` + one new `package.json` script,
`db:verify-clients-crm`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-CONTEXT.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-VALIDATION.md

<interfaces>
`scripts/verify-agent-rls.ts` is the closest existing analog — read its
FULL structure (seed under a randomized `verify-*` agency id, every scope
opened via `withResolvedIdentityContext`/`withSystemWebhookContext`, a
`check(label, condition, detail)` helper accumulating `failures[]`, a
`finally` block that deletes the seeded agency and tolerates the expected
"permission denied" on `audit_log` cascade, `process.exitCode = 1` when
`failures.length > 0`). Copy this exact scaffold, not a new one.

`createClient(params: {agencyId, identity, input}): Promise<CreateClientResult>`
and `updateClient(params: {agencyId, identity, clientId, input}):
Promise<UpdateClientResult>` — `lib/clients/create-client.ts` /
`lib/clients/update-client.ts` (plan 05-03), both tsx-safe.

`classifyAndExecute(toolUse: {id, name, input}, actor: TurnActor):
Promise<ToolResultBlockParam>` and `TOOL_TO_CATALOG_CODE` —
`lib/agent/risk-interceptor.ts` (plan 05-02). `executeTool`/`AGENT_TOOLS` —
`lib/agent/tools/index.ts` (plan 05-04).

`clients_insert_by_team_member` / `clients_update_by_role` /
`clients_delete_admin_only` — the 3 policies plan 05-01 creates in
migration 0021, replacing `clients_write_admin_only`.

`agent_action_catalog` rows seeded by migration 0022: `create_client`,
`update_client`, `list_clients`, `get_client`, all `risk_level = 'low'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify-clients-crm.ts + package.json wiring</name>
  <read_first>
    - scripts/verify-agent-rls.ts (the full file — the scaffold, the check()/expectThrows()/attemptIdentityInsertShouldFail() helpers, the seed-under-randomized-agency-id + finally-cleanup convention)
    - scripts/verify-agent.ts (lines ~400-530 — the exact shape of calling classifyAndExecute directly with a hand-built toolUse + TurnActor, for this script's interceptor assertions)
    - lib/clients/create-client.ts, lib/clients/update-client.ts (plan 05-03 — exact signatures/error unions)
    - lib/agent/tools/create-client.ts (plan 05-04 — exact execute() return strings for the D-07/D-09 assertions)
    - lib/agent/risk-interceptor.ts (plan 05-02 — CLIENT_SCOPED_TOOLS, TOOL_TO_CATALOG_CODE)
    - package.json (the existing db:verify-* script naming convention)
  </read_first>
  <files>scripts/verify-clients-crm.ts, package.json</files>
  <action>
Create `scripts/verify-clients-crm.ts` using `verify-agent-rls.ts`'s exact
scaffold (randomized `verify-clients-crm-{uuid}` agency id, a `check()`
helper, `failures[]`, `try/finally` cleanup tolerating the same
`audit_log`-cascade permission-denied pattern if this script also writes
`audit_log` rows via `classifyAndExecute`). Seed: one agency, one admin
team member, two "member"-role team members (`memberAssigned`,
`memberOther`), one client ("Client A") created by the admin and assigned
to `memberAssigned` only, one `client_contact` identity for a different
client ("Client B") with `optInConfirmedByTeam: true`.

Assertion groups, each a `console.log` section header like the existing
script:

1. **RLS write split (plan 05-01).** `memberOther` (unassigned to
   anything) calls `withResolvedIdentityContext` and inserts directly into
   `clients` — must succeed (INSERT is any-team-member per
   `clients_insert_by_team_member`), proving D-13. `memberOther` then
   attempts to UPDATE Client A directly — must affect 0 rows (unassigned).
   `memberAssigned` UPDATEs Client A directly — must succeed (assigned).
   `admin` UPDATEs Client A — must succeed.

2. **createClient/updateClient (plan 05-03), via the agent-facing
   functions, not raw SQL.** `memberOther` calls `createClient({agencyId,
   identity: memberOtherIdentity, input: {name: "...", phone: "..."}})` —
   expect `success:true`, then assert (via a direct RLS-scoped read as
   `memberOther`) that the new client is now visible to `memberOther`
   (self-assignment proof, D-13). `createClient` called twice with the
   identical name — the second call's `duplicateWarning` must be non-null
   (D-09). `createClient` is NOT called here with missing phone/email —
   that ask-back behavior lives one layer up, in the TOOL (group 4 below);
   this group only proves the service layer's own `contactRequired`
   rejection when called directly with neither field. `updateClient` by
   `memberAssigned` on Client A with a partial patch (`{notes: "nueva
   nota"}`) — expect `success:true`, then read the row back and assert
   `phone`/`email` are UNCHANGED from setup (merge-not-replace proof).
   `updateClient` by `memberOther` targeting Client A — expect
   `success:false, error:"notFound"` (unassigned).

3. **Interceptor (plan 05-02).** Build a `toolUse` for `create_client`
   with `input: {name: "Cliente Interceptor", phone: "+573000000001"}` (NO
   clientId field at all) and a `TurnActor` for `memberOther` — call
   `classifyAndExecute` directly and assert the returned tool_result is
   NOT `is_error` (proving the conditional-clientId fix; this used to be
   unconditionally rejected). Same for a `list_clients` toolUse with
   `input: {}`. Then build an `update_client` toolUse with a `clientId`
   pointing at Client A and a `TurnActor` whose identity is the Client-B
   `client_contact` — assert the result IS `is_error` (the scope
   cross-check must still reject this, proving `update_client` stayed in
   `CLIENT_SCOPED_TOOLS`). Same for `get_client`.

4. **create_client tool D-07/D-09 (plan 05-04), via `executeTool`
   directly.** Call `executeTool("create_client", {name: "Sin Contacto"},
   memberActor)` — assert the returned string equals exactly "Falta un
   dato de contacto: pide teléfono o correo antes de crear el cliente." and
   that no new `clients` row with that name exists afterward (read it back
   inside the same identity scope). Call `executeTool("create_client",
   {name: "Client A", phone: "+573000000002"}, memberActor)` (name matches
   the seeded Client A) — assert the returned string contains "Ya existe
   un cliente llamado".

5. **Catalog classification (D-08/D-17).** A plain `db.select()` against
   `agentActionCatalog` for the 4 new codes — assert all 4 rows exist with
   `riskLevel === "low"`.

Finally block: delete the seeded agency (same cascade-permission-tolerant
try/catch as `verify-agent-rls.ts`, since this script also writes
`audit_log` via `classifyAndExecute`'s low branch).

Add `"db:verify-clients-crm": "tsx scripts/verify-clients-crm.ts"` to
`package.json`'s `scripts`, immediately after the existing
`"db:verify-agent"` line.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the script itself must typecheck; it cannot be RUN against real Neon in this environment, that is plan 05-10's job)
    - `grep -q "db:verify-clients-crm" package.json` exits 0
    - `grep -c "check(" scripts/verify-clients-crm.ts` reports at least 15 (a real assertion count, not a token gesture)
    - `grep -q "clients_insert_by_team_member\|memberOther" scripts/verify-clients-crm.ts` exits 0
    - `grep -q "duplicateWarning" scripts/verify-clients-crm.ts` exits 0
    - `grep -q "Falta un dato de contacto" scripts/verify-clients-crm.ts` exits 0
    - `grep -q "classifyAndExecute(" scripts/verify-clients-crm.ts` exits 0
    - `grep -q "is_error" scripts/verify-clients-crm.ts` exits 0
    - `grep -q "process.exitCode = 1" scripts/verify-clients-crm.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>scripts/verify-clients-crm.ts exists, typechecks, and its assertions cover the RLS write split, the merge-based partial update, the interceptor's conditional clientId fix, D-07/D-09, and the 4 new catalog rows — ready for plan 05-10 to run against real Neon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification script → real Neon (when run in plan 05-10) | Seeds and deletes a randomized test agency; must not leak rows on failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Tampering | Test data left in the real database after a failed run | mitigate | `try/finally` cleanup, same pattern as every prior `verify-*.ts` script; plan 05-10's own acceptance criteria re-confirms a zero-residue count. |
| T-05-23 | Repudiation | A "green" report that does not actually exercise the two research-flagged blockers | mitigate | The grep gates in this plan's acceptance_criteria require the script to literally reference `clients_insert_by_team_member`/`memberOther`, `classifyAndExecute(`, and `is_error` — a script that merely imports these modules without asserting against them would fail the gate. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx eslint .` exits 0
- The script is NOT run against real Neon in this plan — plan 05-10 is the live run
</verification>

<success_criteria>
- scripts/verify-clients-crm.ts exists, typechecks, and covers every Wave-0 gap 05-VALIDATION.md names
- package.json has a new db:verify-clients-crm script
</success_criteria>

<output>
After completion, create `.planning/phases/05-gestion-clientes-crm-conversacional/05-08-SUMMARY.md`
</output>
