---
phase: 05-gestion-clientes-crm-conversacional
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - lib/agent/tools/create-client.ts
  - lib/agent/tools/update-client.ts
  - lib/agent/tools/list-clients.ts
  - lib/agent/tools/get-client.ts
  - lib/agent/tools/index.ts
autonomous: true
requirements: [CLI-01, CLI-03, CLI-04]

must_haves:
  truths:
    - "A team member can dictate a new client to the agent; if phone/email is missing, the agent asks back instead of creating"
    - "A team member can dictate an edit to an existing client and only the mentioned fields change"
    - "A team member can ask the agent to find/list clients by name, industry, or notes text, scoped to what that member can already see"
    - "A team member can ask the agent for a client's full ficha, including notes"
    - "A client_contact or unknown sender is never offered any of these 4 tools"
  artifacts:
    - path: "lib/agent/tools/index.ts"
      provides: "AGENT_TOOLS extended to 6 entries; toolsFor/executeTool unchanged"
      exports: ["AGENT_TOOLS", "toolsFor", "executeTool"]
    - path: "lib/agent/tools/create-client.ts"
      provides: "create_client tool: catalogCode, definition, execute — D-07 ask-back, D-09 duplicate relay"
      exports: ["catalogCode", "definition", "execute"]
    - path: "lib/agent/tools/list-clients.ts"
      provides: "list_clients tool: no clientId field, own inline RLS-scoped query (does not import lib/clients/list-clients.ts)"
      exports: ["catalogCode", "definition", "execute"]
  key_links:
    - from: "lib/agent/tools/index.ts"
      to: "lib/agent/risk-interceptor.ts"
      via: "catalogCode strings here must exactly match TOOL_TO_CATALOG_CODE's values from plan 05-02"
      pattern: "catalogCode = \"(create_client|update_client|list_clients|get_client)\""
    - from: "lib/agent/tools/create-client.ts"
      to: "lib/clients/create-client.ts"
      via: "the tool's execute() calls the agent-facing createClient(), never createClientAction()"
      pattern: "createClient\\("
---

<objective>
Wire the 4 new tool codes into the agent's actual tool registry, following
`draft-client-content.ts`'s exact shape (manual input narrowing, no
validation library, Spanish one-line result strings). `list_clients` and
`get_client` deliberately do NOT import `lib/clients/list-clients.ts` /
`lib/clients/get-client.ts` — both of those files are web-only
(`server-only`, `withTenantContext`) and would crash this module under
`tsx` (plan 05-03's `<interfaces>` explains why). Each tool here opens its
own `withResolvedIdentityContext` scope directly.

Purpose: this is the actual CLI-01/CLI-03/CLI-04 dictation surface — the
part of the phase goal that says "se da de alta un cliente dictándoselo al
agente."
Output: 4 new tool files + `AGENT_TOOLS` extended from 2 to 6 entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-CONTEXT.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-RESEARCH.md
@.planning/phases/05-gestion-clientes-crm-conversacional/05-PATTERNS.md

<interfaces>
From `lib/agent/tools/draft-client-content.ts` (copy this shape exactly for
every new tool file): `export const catalogCode: string`; `export const
definition: Anthropic.Messages.Tool` (`name`, Spanish `description`,
`input_schema: { type: "object", properties: {...}, required: [...] }`); a
local, non-exported `type FooInput = {...}` and `function isValidInput(input:
unknown): input is FooInput` (hand-written narrowing — no zod, repo
posture); `export async function execute(input: unknown, actor: TurnActor):
Promise<string>`.

From plan 05-03 (already built): `createClient(params: { agencyId: string;
identity: ResolvedIdentity; input: CreateClientInput }): Promise<{success:
true; clientId: string; duplicateWarning: string | null} | {success: false;
error: "nameRequired"|"contactRequired"|"invalidIndustry"|"forbidden"|"unknown"}>`;
`updateClient(params: { agencyId; identity; clientId: string; input:
UpdateClientInput }): Promise<{success:true} | {success:false; error:
"nameRequired"|"contactRequired"|"invalidIndustry"|"notFound"|"forbidden"|"unknown"}>`;
`CreateClientInput = { name: string; phone?; email?; industry?; notes? }`;
`UpdateClientInput = Partial<{ name; phone; email; industry; notes }>` (all
strings).

`withResolvedIdentityContext<T>(agencyId, identity, fn: (tx) =>
Promise<T>): Promise<T>` — `lib/tenant/with-resolved-identity-context.ts`.

`clients` schema (`lib/db/schema/clients.ts`, extended by plan 05-01): `id,
agencyId, name, phone, email, industry, notes, createdAt`. Import `ilike,
or, eq` from `drizzle-orm` for the two read tools' own inline queries.

`TurnActor` (`lib/agent/types.ts`): `{ agencyId: string; identity:
ResolvedIdentity; clientId: string | null; channel; counterpartPhoneNumber
}`.

`lib/agent/tools/index.ts`'s current shape (read the full file, 24 lines):
`AGENT_TOOLS` is a `readonly` array of tool modules; `toolsFor(identity)`
returns `AGENT_TOOLS.map(t => t.definition)` only for `identity.type ===
"team_member"`; `executeTool(name, input, actor)` looks the tool up by
`definition.name` and returns a Spanish "unknown tool" string on a miss,
never throws.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: create_client and update_client tools</name>
  <read_first>
    - lib/agent/tools/draft-client-content.ts (the exact file shape to copy)
    - lib/clients/create-client.ts and lib/clients/update-client.ts (plan 05-03 — the exact functions this task calls)
    - lib/clients/industries.ts (the 8 valid codes, for the tool's own description text)
    - .planning/phases/05-gestion-clientes-crm-conversacional/05-RESEARCH.md, "Pattern 4: Agent tool shape" (create_client's full drafted execute() logic, including D-07's ask-back text and D-09's duplicate relay — ready to adapt almost verbatim)
  </read_first>
  <behavior>
    - create_client's execute() returns the D-07 ask-back message and does not call createClient when both phone and email are absent
    - create_client's execute() returns a success message including the duplicateWarning text when one is present
    - update_client's execute() requires clientId and at least one other field; returns a Spanish "nothing to change" message if only clientId is given
    - update_client's execute() maps each UpdateClientError to a distinct Spanish sentence, including notFound
  </behavior>
  <files>lib/agent/tools/create-client.ts, lib/agent/tools/update-client.ts</files>
  <action>
`lib/agent/tools/create-client.ts`: `catalogCode = "create_client"`.
`definition.name = "create_client"`, Spanish description mentioning it
requires a name and at least a phone or an email. `input_schema.properties`:
`name` (string, required), `phone` (string, optional), `email` (string,
optional), `industry` (string, optional, description lists the 8 valid
codes from `CLIENT_INDUSTRY_CODES` inline in the tool description text so
the model knows the exact codes to use), `notes` (string, optional).
`required: ["name"]`. `isValidInput` narrows `name` as required string, the
other 4 as optional strings. `execute(input, actor)`: invalid input ->
Spanish "se requiere al menos name" message; if `!input.phone &&
!input.email` -> return exactly "Falta un dato de contacto: pide teléfono o
correo antes de crear el cliente." (D-07) and do NOT call `createClient`;
otherwise `await createClient({ agencyId: actor.agencyId, identity:
actor.identity, input })`; on success, build `` `Cliente ${name} creado.` ``
and append the `duplicateWarning` text (space-separated) when it is
non-null; on failure, map each `CreateClientError` to one distinct Spanish
sentence (`nameRequired`, `contactRequired`, `invalidIndustry`,
`forbidden`, `unknown`).

`lib/agent/tools/update-client.ts`: `catalogCode = "update_client"`.
`definition.name = "update_client"`. `input_schema.properties`: `clientId`
(string, required, "UUID del cliente"), plus the same optional `name`/
`phone`/`email`/`industry`/`notes` as create, `required: ["clientId"]`.
`isValidInput` narrows `clientId` as required string, the rest optional
strings. `execute(input, actor)`: invalid input -> Spanish message; if none
of `name`/`phone`/`email`/`industry`/`notes` is present -> return "No
especificaste qué cambiar del cliente." without calling `updateClient`;
otherwise `await updateClient({ agencyId: actor.agencyId, identity:
actor.identity, clientId: input.clientId, input: { the provided fields
only } })`; on success return "Cliente actualizado."; on failure map every
`UpdateClientError` (including `notFound`, phrased as "No encontré ese
cliente o no tenés acceso a él.") to a distinct Spanish sentence.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -q 'catalogCode = "create_client"' lib/agent/tools/create-client.ts` exits 0
    - `grep -q 'catalogCode = "update_client"' lib/agent/tools/update-client.ts` exits 0
    - `grep -q "Falta un dato de contacto" lib/agent/tools/create-client.ts` exits 0
    - `grep -q "createClient(" lib/agent/tools/create-client.ts` exits 0 and `grep -q "createClientAction" lib/agent/tools/create-client.ts` reports no match
    - `grep -q "updateClient(" lib/agent/tools/update-client.ts` exits 0 and `grep -q "updateClientAction" lib/agent/tools/update-client.ts` reports no match
    - `grep -q "zod" lib/agent/tools/create-client.ts lib/agent/tools/update-client.ts` reports no match
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>create_client and update_client tools exist, both call only the agent-facing (identity-based) service functions, D-07/D-09 behavior is implemented exactly as specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: list_clients and get_client tools + registry</name>
  <read_first>
    - lib/agent/tools/send-payment-reminder.ts (the tool-file shape to adapt for the definition/narrowing skeleton)
    - lib/db/schema/clients.ts (columns to select and to filter on)
    - lib/tenant/with-resolved-identity-context.ts (the exact scope every read here must open)
    - lib/agent/tools/index.ts (the current registry — extend in place, do not restructure toolsFor/executeTool)
    - .planning/phases/05-gestion-clientes-crm-conversacional/05-RESEARCH.md, "Anti-Patterns to Avoid" (do not add a fake clientId field to list_clients just to satisfy the interceptor — plan 05-02 already fixed the interceptor itself)
  </read_first>
  <behavior>
    - list_clients' execute() with no query returns every RLS-visible client (capped) formatted as a short list
    - list_clients' execute() with a query returns only rows whose name/industry/notes case-insensitively contain it
    - list_clients' execute() with zero matches returns a Spanish "no results" sentence, not an empty string
    - get_client's execute() with a valid, visible clientId returns name/phone/email/industry/notes
    - get_client's execute() with a clientId outside the caller's RLS scope returns a Spanish "not found or no access" sentence, never throws
    - toolsFor(team_member) now returns 6 tool definitions; toolsFor(client_contact) and toolsFor(unknown) still return an empty array
  </behavior>
  <files>lib/agent/tools/list-clients.ts, lib/agent/tools/get-client.ts, lib/agent/tools/index.ts</files>
  <action>
`lib/agent/tools/list-clients.ts`: `catalogCode = "list_clients"`.
`definition.name = "list_clients"`, description: searches clients by
name/industry/notes, returns only what the caller can already see, empty
query lists everyone visible. `input_schema.properties: { query: { type:
"string", description: "Texto a buscar... opcional" } }`, `required: []` —
deliberately no `clientId` field (per the Anti-Patterns note).
`isValidInput` accepts `{}` or `{ query?: string }`. `execute(input,
actor)`: open `withResolvedIdentityContext(actor.agencyId, actor.identity,
tx => ...)` doing its OWN query directly against the `clients` schema
(import `clients` from `@/lib/db/schema/clients`, `ilike`/`or` from
`drizzle-orm`) — select `id, name, phone, email, industry`, filter with the
same `ilike(name/industry/notes, %query%)` OR-shape as
`lib/clients/list-clients.ts`'s web-facing filter when `query` is
non-blank, order by name, `limit(20)` to bound response size. Format each
row as one line: name, industry-or-"sin industria" in parentheses,
phone-or-email-or-"sin contacto". Empty result -> "No se encontraron
clientes." Do NOT import anything from `lib/clients/list-clients.ts` (that
file is `server-only`/web-only, importing it here would crash this module
under `tsx` and inside Inngest — see plan 05-03's `<interfaces>` for why).

`lib/agent/tools/get-client.ts`: `catalogCode = "get_client"`.
`definition.name = "get_client"`. `input_schema.properties: { clientId: {
type: "string", description: "UUID del cliente" } }`, `required:
["clientId"]`. `isValidInput` narrows `clientId` as required string.
`execute(input, actor)`: open `withResolvedIdentityContext(actor.agencyId,
actor.identity, tx => tx.select().from(clients).where(eq(clients.id,
input.clientId)).limit(1))` (own inline query, same reason as above — do
NOT import `lib/clients/get-client.ts`); no row -> "No encontré ese cliente
o no tenés acceso a él."; a row -> format name, phone, email, industry, and
notes (notes ARE included here — D-06's team-only layering restricts the
CLIENT-facing surface, not this team-member-only tool) into one readable
Spanish block.

`lib/agent/tools/index.ts`: add `import * as createClientTool from
"./create-client"`, `import * as updateClientTool from "./update-client"`,
`import * as listClientsTool from "./list-clients"`, `import * as
getClientTool from "./get-client"`; append all 4 to the `AGENT_TOOLS` array
(order does not matter to `toolsFor`/`executeTool`, both iterate
generically — no other change to this file).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -q "clientId" lib/agent/tools/list-clients.ts` reports no match inside `input_schema.properties` (the tool truly takes no clientId — a plain grep for the literal string `clientId:` in the file returns no result)
    - `grep -q "from \"@/lib/clients/list-clients\"" lib/agent/tools/list-clients.ts` reports no match
    - `grep -q "from \"@/lib/clients/get-client\"" lib/agent/tools/get-client.ts` reports no match
    - `grep -q "No se encontraron clientes" lib/agent/tools/list-clients.ts` exits 0
    - `grep -c "createClientTool\|updateClientTool\|listClientsTool\|getClientTool" lib/agent/tools/index.ts` reports at least 4
    - `node -e "require('tsx/cjs');const t=require('./lib/agent/tools/index.ts');console.log(t.AGENT_TOOLS.length)"` is NOT required to run here (tsx/cjs loader may not be configured) — rely on the tsc gate plus the grep gates above instead
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>AGENT_TOOLS has 6 entries; list_clients and get_client never import the web-only lib/clients read modules; toolsFor/executeTool behavior is unchanged for the 2 pre-existing tools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model tool-call arguments → clients table filters | list_clients'/get_client's own query construction must not introduce a second, driftable copy of RLS scoping logic |
| Notes field exposure | get_client returns notes to the requesting team_member — never reachable by a client_contact, since toolsFor() offers no tools to that identity type |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Elevation of Privilege | list_clients'/get_client's own inline query bypassing RLS | mitigate | Both open `withResolvedIdentityContext` before querying; `clients_select_by_role` (unchanged this phase) is the only scope filter — no manual `WHERE agency_id`/role predicate is added in either tool. |
| T-05-13 | Information Disclosure | get_client leaking team-only notes to a client-facing surface | accept (by construction) | `toolsFor()` never offers any tool, including get_client, to a `client_contact` or `unknown` identity — enforced upstream of this plan (LD-12, Phase 4). This plan adds no new exception to that rule. |
| T-05-14 | Denial of Service (token/cost) | list_clients returning unbounded rows into the model context | mitigate | `.limit(20)` caps the query; a caller needing more should narrow the `query` text instead. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx eslint .` exits 0
- `npm run verify:agent-prompt` still exits 0 (no regression to the existing 2-tool behavior)
- No live-database assertion here — plan 05-08 writes the integration test, plan 05-10 runs it against real Neon
</verification>

<success_criteria>
- AGENT_TOOLS has 6 entries: the 2 existing plus create_client/update_client/list_clients/get_client
- Every new tool's catalogCode matches plan 05-02's TOOL_TO_CATALOG_CODE keys exactly
- list_clients and get_client never import the web-only lib/clients/{list-clients,get-client}.ts modules
</success_criteria>

<output>
After completion, create `.planning/phases/05-gestion-clientes-crm-conversacional/05-04-SUMMARY.md`
</output>
