---
phase: 03-integraci-n-con-whatsapp-meta
plan: 08
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06", "03-07"]
files_modified:
  - scripts/verify-whatsapp-webhook.ts
autonomous: false
requirements: [WA-01, WA-03, WA-04, WA-05, WA-07]

user_setup:
  - service: neon-postgres
    why: "Migrations 0012/0013 must be applied to the real database before any integration assertion can run"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Project -> Connection Details -> pooled connection string (owner role, needs DDL rights)"
  - service: meta-whatsapp-cloud-api
    why: "D-01 requires a real inbound message to produce a real outbound ack through Meta's API"
    env_vars:
      - name: META_APP_SECRET
        source: "developers.facebook.com -> App Settings -> Basic -> App Secret"
      - name: META_WEBHOOK_VERIFY_TOKEN
        source: "Self-chosen; must match the value entered in the App Dashboard webhook config"
      - name: META_WHATSAPP_PHONE_NUMBER_ID
        source: "developers.facebook.com -> WhatsApp -> API Setup -> Phone number ID"
      - name: META_WHATSAPP_ACCESS_TOKEN
        source: "Business Settings -> Users -> System Users -> long-lived token with whatsapp_business_messaging"
    dashboard_config:
      - task: "Register the public HTTPS callback URL and verify token, then subscribe the app to the `messages` field"
        location: "developers.facebook.com -> WhatsApp -> Configuration -> Webhook"
      - task: "Confirm the WABA-to-App subscription actually persisted via POST/GET /{WABA_ID}/subscribed_apps"
        location: "Graph API Explorer or curl, with a token carrying whatsapp_business_management"
      - task: "Add every phone number used in testing to the test number's allowed recipient list (hard cap 5)"
        location: "developers.facebook.com -> WhatsApp -> API Setup -> 'To' field management"

must_haves:
  truths:
    - "The messages table, its RLS policies and the SECURITY DEFINER function exist in the real Neon database"
    - "A team member's WhatsApp number resolves to their own agency across agencies, and an unregistered number resolves to nothing"
    - "The SECURITY DEFINER function returns exactly one scalar column"
    - "An unknown identity scope reads zero rows from messages"
    - "A duplicate Meta message id produces exactly one row"
    - "Phase 1 and Phase 2 verification suites still pass after migrations 0012/0013"
    - "A real WhatsApp message sent to the test number produces a real ack on the sender's phone"
  artifacts:
    - path: "scripts/verify-whatsapp-webhook.ts"
      provides: "Real-Neon integration proof of the whole inbound pipeline"
  key_links:
    - from: "scripts/verify-whatsapp-webhook.ts"
      to: "lib/whatsapp/ingest-inbound-message.ts"
      via: "calls the real production function, not a reimplementation"
      pattern: "ingestInboundMessage\\("
    - from: "npm run db:migrate"
      to: "drizzle/migrations/0013_whatsapp_messages_rls.sql"
      via: "journal-driven migrator against real Neon"
      pattern: "0013_whatsapp_messages_rls"
---

<objective>
Prove the phase against reality: apply migrations 0012 and 0013 to the real
Neon database, run a real-Neon integration suite over the whole inbound
pipeline, confirm no Phase 1/2 regression, and observe a real WhatsApp
message produce a real ack.

Purpose: everything before this plan typechecks and lints, which for a
database- and third-party-integration phase proves almost nothing. TypeScript
types come from the schema file, not the live database, so a build passing
without the migration applied is a false positive.
Output: one integration script, one applied migration pair, and two human
confirmations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integraci-n-con-whatsapp-meta/03-CONTEXT.md
@.planning/phases/03-integraci-n-con-whatsapp-meta/03-RESEARCH.md
@.planning/phases/03-integraci-n-con-whatsapp-meta/03-PATTERNS.md
@.planning/phases/03-integraci-n-con-whatsapp-meta/03-VALIDATION.md
@.planning/phases/02-modelo-identidad-permisos/02-07-SUMMARY.md

<interfaces>
Functions under test (call the real ones; never reimplement their logic in
the script):

```typescript
// lib/whatsapp/find-agency-by-whatsapp-number.ts
export async function findAgencyByTeamWhatsAppNumber(phoneNumber: string): Promise<string | null>;

// lib/whatsapp/resolve-inbound-agency.ts
export async function resolveInboundAgency(phoneNumberId: string, fromPhoneNumber: string): Promise<InboundAgencyResolution>;
export function resetAgencyNumberMapCache(): void;

// lib/whatsapp/ingest-inbound-message.ts
export async function ingestInboundMessage(
  message: ParsedInboundMessage, phoneNumberId: string, platformPhoneNumber: string,
): Promise<IngestResult>;

// lib/whatsapp/record-delivery-status.ts
export async function recordDeliveryStatus(status: ParsedStatusUpdate, phoneNumberId: string): Promise<"updated" | "no_match" | "no_agency">;

// lib/tenant/with-system-webhook-context.ts
export async function withSystemWebhookContext<T>(agencyId: string, fn: (tx: IdentityTx) => Promise<T>): Promise<T>;

// lib/tenant/with-resolved-identity-context.ts (Phase 2)
export async function withResolvedIdentityContext<T>(agencyId: string, identity: ResolvedIdentity, fn: (tx: IdentityTx) => Promise<T>): Promise<T>;
```

Harness to reuse verbatim from `scripts/verify-identity-resolution.ts`:
- `failures` array, `check()`, `assertRowCount()`, `errorMessage()` (lines 43-70)
- `attemptInsertShouldFail()` and `expectThrows()` (lines 76-112)
- the `batch()` wrapper with its `eslint-disable @typescript-eslint/no-explicit-any` comment (lines 114-121)
- randomized test ids: `` `verify-whatsapp-agency-a-${randomUUID()}` ``

Seeding pattern: `db.batch([...])` with the `set_config` calls in the SAME
batch as the write — this script IS the privileged setup path, there is no
admin API. `authorized_contacts` writes additionally require
`set_config('app.role', 'admin', true)` in the same batch (migration 0006).

Existing regression suites that must not break:
`npm run db:verify-rls` (Phase 1, expects 7/7) and
`npm run db:verify-identity` (Phase 2, expects 16/18 — see
`02-07-SUMMARY.md` for the two known open failures and why they are open).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the real-Neon integration suite for the inbound pipeline</name>

  <read_first>
    - scripts/verify-identity-resolution.ts (read the WHOLE file: helper trio, batch() wrapper, seeding-with-set_config pattern, cleanup, footer)
    - scripts/verify-rls-isolation.ts (the Phase 1 analog, for the raw-RLS assertion style)
    - lib/whatsapp/ingest-inbound-message.ts, lib/whatsapp/resolve-inbound-agency.ts, lib/whatsapp/find-agency-by-whatsapp-number.ts, lib/whatsapp/record-delivery-status.ts (the modules under test)
    - lib/tenant/with-system-webhook-context.ts (used directly in RLS assertions)
    - drizzle/migrations/0013_whatsapp_messages_rls.sql (the policies being asserted)
    - .planning/phases/03-integraci-n-con-whatsapp-meta/03-VALIDATION.md (the requirement-to-assertion map this script must satisfy)
  </read_first>

  <files>scripts/verify-whatsapp-webhook.ts</files>

  <action>
Create `scripts/verify-whatsapp-webhook.ts`, wired by plan 03-02 as
`npm run db:verify-whatsapp`. Structure it exactly like
`scripts/verify-identity-resolution.ts`.

1. Header doc comment covering: this is the real-Neon half of Phase 3's test
   matrix (the network-free half is `verify-whatsapp-webhook-parsing.ts` and
   `verify-whatsapp-send.ts`); it calls the REAL production functions so the
   proof is that Postgres RLS confines each actor, not that application code
   does; it needs egress to Neon and therefore cannot run in a sandboxed
   session; migrations 0012 and 0013 must already be applied.

2. Copy the helper trio, `expectThrows`, and the `batch()` wrapper verbatim.

3. Test constants — randomize phone numbers so a leftover row from an
   interrupted run cannot collide with the new GLOBAL unique index on
   `team_members.whatsapp_number`:

```typescript
const suffix = randomUUID().replace(/\D/g, "").slice(0, 7).padEnd(7, "0");
const TEAM_PHONE = `+5730${suffix}`;
const CONTACT_PHONE = `+5731${suffix}`;
const STRANGER_PHONE = `+5732${suffix}`;
const SHARED_PNID = `verify-shared-${randomUUID()}`;
const AGENCY_PNID = `verify-agency-${randomUUID()}`;
```

4. Seed: agency A and agency B (randomized ids), client A1 in agency A, one
   team member in agency A with `whatsappNumber: TEAM_PHONE`, one
   `authorized_contacts` row in agency A for client A1 with
   `phoneNumber: CONTACT_PHONE` and `optInConfirmedByTeam: true` plus the
   confirmer/timestamp the CHECK constraint requires.

5. Configure routing in-process before the first call:

```typescript
process.env.META_WHATSAPP_PHONE_NUMBER_ID = SHARED_PNID;
process.env.META_AGENCY_PHONE_NUMBER_MAP = JSON.stringify({ [AGENCY_PNID]: agencyAId });
resetAgencyNumberMapCache();
```

   Add a comment noting this stands in for what WA-02's Embedded Signup will
   persist in the database — a test-harness configuration of the real
   production code path, not a separate code path.

6. Assertions — all of the following, each with a numbered label carrying the
   requirement or threat id, e.g. `"(3) T-03-04: ..."`:

   Cross-agency lookup (WA-04):
   - (1) `findAgencyByTeamWhatsAppNumber(TEAM_PHONE)` returns `agencyAId`.
   - (2) `findAgencyByTeamWhatsAppNumber(STRANGER_PHONE)` returns `null`.
   - (3) T-03-04: the function returns a single scalar. Assert via
     `select prosecdef, pg_get_function_result(oid) as result_type, pg_get_functiondef(oid) as def from pg_proc where proname = 'find_agency_by_team_whatsapp_number'`:
     `prosecdef === true`, `result_type === 'text'`, and `def` contains
     `search_path` and does NOT contain the substrings `role`, `email`, or
     `RETURNS TABLE`.
   - (4) T-03-10: seeding a team member in agency B with the SAME
     `TEAM_PHONE` fails. Use `expectThrows` with the fragment
     `team_members_whatsapp_number_global_idx`.

   Routing (WA-03/WA-04):
   - (5) `resolveInboundAgency(SHARED_PNID, TEAM_PHONE)` returns
     `{ kind: "shared_internal", agencyId: agencyAId }`.
   - (6) `resolveInboundAgency(SHARED_PNID, STRANGER_PHONE)` returns
     `{ kind: "no_agency", reason: "unknown_sender_on_shared_number" }`.
   - (7) `resolveInboundAgency(AGENCY_PNID, STRANGER_PHONE)` returns
     `{ kind: "agency_number", agencyId: agencyAId }`.
   - (8) `resolveInboundAgency("not-a-configured-id", TEAM_PHONE)` returns
     `{ kind: "no_agency", reason: "unmapped_phone_number_id" }`.

   Ingestion and identity (the phase success criterion, D-04):
   - (9) A text message from `TEAM_PHONE` on `SHARED_PNID` returns
     `outcome: "ingested"`; the stored row has `agencyId === agencyAId`,
     `resolvedIdentityType === "team_member"`, `resolvedIdentityId` equal to
     the seeded team member id, `clientId === null`, `direction === "inbound"`,
     `channel === "whatsapp"`, `textBody` equal to the sent text.
   - (10) D-04 idempotency / T-03-03: re-ingesting the SAME `metaMessageId`
     returns `outcome: "duplicate"`, and a count of rows for that
     `metaMessageId` is exactly 1.
   - (11) A text message from `CONTACT_PHONE` on `AGENCY_PNID` yields
     `resolvedIdentityType === "client_contact"` with `clientId` equal to
     client A1's id and `resolvedIdentityId` equal to the contact row id.
   - (12) A text message from `STRANGER_PHONE` on `AGENCY_PNID` yields
     `outcome: "ingested"` with `resolvedIdentityType === "unknown"`,
     `resolvedIdentityId === null` and `clientId === null` — D-04's unknown
     branch still produces a row when an agency IS known.
   - (13) A message from `STRANGER_PHONE` on `SHARED_PNID` returns
     `outcome: "no_agency"`, and a `SELECT count(*)` over `messages` for both
     agencies is unchanged by that call.
   - (14) D-05: an image message (kind `"media"`, `mediaType: "image"`,
     `mediaId`, `mimeType: "image/jpeg"`) stores `messageType === "image"`,
     `mediaId` and `mediaMimeType` populated, and `textBody === null`.

   RLS (T-03-05 / SEG-12, T-03-18):
   - (15) Inside `withResolvedIdentityContext(agencyAId, { type: "unknown" }, ...)`,
     `select * from messages` returns ZERO rows.
   - (16) Inside `withResolvedIdentityContext(agencyAId, { type: "team_member",
     teamMemberId, role: "admin" }, ...)`, the same select returns the seeded
     agency-A rows (count greater than 0).
   - (17) Inside `withSystemWebhookContext(agencyBId, ...)`, a select over
     `messages` returns ZERO of agency A's rows.
   - (18) Inside `withResolvedIdentityContext(agencyAId, { type: "client_contact",
     contactId, clientId: clientA1Id, optInConfirmedByTeam: true }, ...)`,
     the select returns only rows whose `clientId === clientA1Id` — assert
     that no returned row has a null or different `clientId`.

   WA-07 pricing backfill:
   - (19) Insert an outbound row under `withSystemWebhookContext(agencyAId)`
     with a known `metaMessageId`, then call `recordDeliveryStatus` with
     `{ metaMessageId, recipientPhoneNumber: TEAM_PHONE, status: "delivered",
     conversationId: "conv-verify", pricingCategory: "service",
     pricingBillable: false }` on `SHARED_PNID`. Assert it returns `"updated"`
     and the row now has `deliveryStatus === "delivered"`,
     `conversationId === "conv-verify"`, `pricingCategory === "service"`,
     `pricingBillable === false`.
   - (20) `recordDeliveryStatus` for a `metaMessageId` that does not exist
     returns `"no_match"`.
   - (21) A follow-up status with `pricingCategory: null` and
     `pricingBillable: null` leaves the previously recorded pricing values
     intact (only `deliveryStatus` changes).

7. Cleanup in a `finally`: delete agency A and agency B by id (the FK cascade
   removes `messages`, `team_members`, `clients` and `authorized_contacts`;
   referential-integrity cascades are not subject to RLS). Follow
   `verify-identity-resolution.ts`'s cleanup shape, including its `set_config`
   calls in the same batch.

8. Standard footer: print failures, `process.exitCode = 1` when
   `failures.length > 0`, otherwise "All assertions passed."
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx eslint scripts/verify-whatsapp-webhook.ts</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/verify-whatsapp-webhook.ts` exists, typechecks and lints clean
    - It imports and calls the real `ingestInboundMessage`, `resolveInboundAgency`, `findAgencyByTeamWhatsAppNumber`, `recordDeliveryStatus`, `withSystemWebhookContext` and `withResolvedIdentityContext` — `grep -c 'from "../lib/' scripts/verify-whatsapp-webhook.ts` returns at least 6
    - `grep -q 'randomUUID()' scripts/verify-whatsapp-webhook.ts` exits 0 and no hardcoded phone-number literal like `"+573001110001"` appears (the global unique index makes fixed numbers collide across runs)
    - `grep -q 'pg_get_functiondef' scripts/verify-whatsapp-webhook.ts` exits 0 (T-03-04 assertion present)
    - `grep -q 'team_members_whatsapp_number_global_idx' scripts/verify-whatsapp-webhook.ts` exits 0
    - `grep -q 'resetAgencyNumberMapCache()' scripts/verify-whatsapp-webhook.ts` exits 0
    - `grep -q 'process.exitCode = 1' scripts/verify-whatsapp-webhook.ts` exits 0
    - Cleanup runs in a `finally` block: `grep -q 'finally {' scripts/verify-whatsapp-webhook.ts` exits 0
    - The script contains at least 21 distinct `check(` labels
  </acceptance_criteria>

  <done>A single command can prove the whole inbound pipeline against real Neon, including the RLS boundaries the phase's design depends on.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migrations 0012/0013 to real Neon and run the full suite</name>

  <read_first>
    - scripts/migrate.ts (the runner; note it needs DATABASE_URL for a role WITH DDL rights, not app_user)
    - drizzle/migrations/0012_whatsapp_messages.sql
    - drizzle/migrations/0013_whatsapp_messages_rls.sql
    - .planning/phases/02-modelo-identidad-permisos/02-07-SUMMARY.md (the identical checkpoint from Phase 2, including the two known db:verify-identity failures)
    - .planning/STATE.md (network egress varies per session — verify directly, never assume it is blocked)
  </read_first>

  <files>drizzle/migrations/0012_whatsapp_messages.sql, drizzle/migrations/0013_whatsapp_messages_rls.sql (applied, not edited)</files>

  <action>
Run the seven-step procedure in `<how-to-verify>` below, in order, from a
session with real network egress to Neon and a `.env` carrying a
`DATABASE_URL` for the Neon owner role (NOT `app_user` — `scripts/migrate.ts`
needs DDL rights).

The executor performs steps 1-7 itself where it has egress; it stops and hands
the whole procedure to the user when `psql "$DATABASE_URL" -c "select 1"` in
step 1 fails. Do not skip step 2 (the stale-duplicate check) — migration 0012
creates a GLOBAL unique index on `team_members.whatsapp_number`, and leftover
rows from an interrupted Phase 2 verification run will make it fail. The fix
is to delete the stale `verify-identity-*` agencies, never to drop the index.

Report the output of step 5 and the pass counts from step 6 verbatim.
  </action>

  <what-built>
Plans 03-01 through 03-07 produced: the `messages` table and its RLS policy
set, the `find_agency_by_team_whatsapp_number` SECURITY DEFINER function, a
global unique index on `team_members.whatsapp_number`, the webhook route
handler, the identity/ingestion pipeline, the outbound send, and the Inngest
ack function.

None of it has touched a real database. This step applies migrations 0012 and
0013 and runs every verification suite. Build and typecheck have been passing
throughout WITHOUT the migration applied, because TypeScript types come from
`lib/db/schema/messages.ts` rather than the live database — treating those
green checks as verification would be a false positive.
  </what-built>

  <how-to-verify>
Run these from a session with real network egress to Neon, with a `.env`
carrying a `DATABASE_URL` for the owner role.

1. Confirm egress actually exists in THIS session (STATE.md notes it varies
   session to session — check, do not assume):
   ```bash
   psql "$DATABASE_URL" -c "select 1"
   ```
   If `psql` is unavailable, `npx tsx -e "import('./lib/db').then(m => m.db.execute('select 1')).then(console.log)"`.

2. Check for stale verification data that the NEW global unique index on
   `team_members.whatsapp_number` could collide with:
   ```bash
   psql "$DATABASE_URL" -c "select whatsapp_number, count(*) from team_members where whatsapp_number is not null group by 1 having count(*) > 1"
   ```
   Expected: zero rows. If any appear, they are leftovers from an interrupted
   Phase 2 verification run — delete the offending `verify-identity-*` /
   `verify-whatsapp-*` agencies before continuing. Do NOT drop the index.

3. Apply the migrations:
   ```bash
   npm run db:migrate
   ```
   Expected: `Migrations applied.` Only 0012 and 0013 should be new; 0000-0011
   are already recorded in `drizzle.__drizzle_migrations`.

4. Confirm the objects exist:
   ```bash
   psql "$DATABASE_URL" -c "\d messages"
   psql "$DATABASE_URL" -c "select polname, polcmd from pg_policy where polrelid = 'messages'::regclass"
   psql "$DATABASE_URL" -c "select proname, prosecdef, pg_get_function_result(oid) from pg_proc where proname = 'find_agency_by_team_whatsapp_number'"
   ```
   Expected: the table with 18 columns; policies `messages_system_webhook_all`
   and `messages_select_by_role`; the function with `prosecdef = t` returning
   `text`.

5. Run the new integration suite:
   ```bash
   npm run db:verify-whatsapp
   ```
   Expected: all 21+ assertions pass, exit code 0.

6. Run the regression suites — migrations 0012/0013 must not have broken
   Phase 1 or Phase 2:
   ```bash
   npm run db:verify-rls        # expect 7/7
   npm run db:verify-identity   # expect 16/18 (the same two known failures documented in 02-07-SUMMARY.md, no new ones)
   ```
   If `db:verify-identity` now fails with a message mentioning
   `team_members_whatsapp_number_global_idx`, that is stale data from step 2,
   not a code defect — clean it and re-run.

7. Run the offline suites once more against the applied schema:
   ```bash
   npx tsx scripts/verify-whatsapp-webhook-parsing.ts
   npx tsx scripts/verify-whatsapp-send.ts
   ```

Report back: the output of step 5, the pass counts from step 6, and any
assertion that failed with its label.
  </how-to-verify>

  <acceptance_criteria>
    - `npm run db:migrate` completes with `Migrations applied.` and no error
    - `pg_policy` lists exactly `messages_system_webhook_all` and `messages_select_by_role` on `messages`
    - `pg_proc` shows `find_agency_by_team_whatsapp_number` with `prosecdef = true` and result type `text`
    - `npm run db:verify-whatsapp` exits 0 with zero `[FAIL]` lines
    - `npm run db:verify-rls` passes 7/7
    - `npm run db:verify-identity` shows no NEW failures beyond the two documented in `02-07-SUMMARY.md`
    - Both offline scripts still exit 0
  </acceptance_criteria>

  <verify>
    <automated>npm run db:migrate && npm run db:verify-whatsapp && npm run db:verify-rls && npm run db:verify-identity</automated>
  </verify>

  <done>Migrations 0012 and 0013 are applied to real Neon, `db:verify-whatsapp` is green, and neither Phase 1 nor Phase 2 regression suite has a new failure.</done>

  <resume-signal>Paste the output of `npm run db:verify-whatsapp` plus the pass counts for `db:verify-rls` and `db:verify-identity`, then type "approved" — or describe the failures.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real end-to-end WhatsApp round trip (D-01)</name>

  <read_first>
    - .planning/phases/03-integraci-n-con-whatsapp-meta/03-RESEARCH.md (Common Pitfalls 1-4: bare challenge string, WABA subscription not persisting, 5-recipient cap, 24h token)
    - app/api/webhooks/meta/route.ts (the handler being exercised)
    - .env.example (the five Meta variables and where each comes from)
    - .planning/phases/01-fundaciones-cuenta-equipo/01-fundaciones-cuenta-equipo-02-SUMMARY.md (the ngrok-based webhook testing pattern this repo already used for Clerk)
  </read_first>

  <files>none (observation only — no file is edited by this task)</files>

  <action>
Run the eight-step procedure in `<how-to-verify>` below against the real Meta
developer test WABA (D-02), from a machine with the user's own Meta
credentials and a public HTTPS tunnel.

This task is human-gated end to end: it requires a physical phone sending a
real WhatsApp message, which no automated command can substitute for. The
executor's job is to have `npm run dev` and the tunnel running, to walk the
user through steps 2-4, and to read back the database state in step 6 with the
`psql` query given there.

Record in the summary anything that diverged from the expected behavior —
specifically whether Meta's retry behavior on the test WABA matched production
expectations (RESEARCH.md Open Question 2) and whether the access token needed
regenerating as a System User token (Pitfall 4).
  </action>

  <what-built>
The complete inbound-plus-outbound pipeline: Meta posts a signed webhook, the
handler verifies the HMAC, resolves the agency and the sender's identity,
persists the message, dispatches an Inngest event, and the background function
sends a fixed ack back through Meta's real send API and records the outbound
row.

D-01 exists precisely because a 200 OK to the webhook proves nothing about
outbound sending. Only a real message arriving on a real phone does.
  </what-built>

  <how-to-verify>
Prerequisites: Task 2 approved (migrations applied, suites green), the five
Meta variables set in `.env`, and the sending phone added to the test number's
allowed-recipient list.

1. Start the app and expose it over HTTPS (localhost is not acceptable to
   Meta — same approach Phase 1 used for the Clerk webhook):
   ```bash
   npm run dev
   ngrok http 3000
   ```

2. Register the webhook: developers.facebook.com -> your App -> WhatsApp ->
   Configuration -> Webhook -> Edit.
   - Callback URL: `https://<ngrok-host>/api/webhooks/meta`
   - Verify token: the exact `META_WEBHOOK_VERIFY_TOKEN` value from `.env`
   - Click "Verify and Save".
   Expected: it saves. If it fails immediately, the GET handler is returning
   something other than the bare `hub.challenge` string, or the tokens differ.

3. Subscribe to the `messages` field in that same Webhooks panel, then
   CONFIRM the WABA-to-App subscription actually persisted (RESEARCH.md
   Pitfall 2 — the dashboard toggle can silently fail to register):
   ```bash
   curl -X POST "https://graph.facebook.com/v25.0/<WABA_ID>/subscribed_apps" -H "Authorization: Bearer $META_WHATSAPP_ACCESS_TOKEN"
   curl "https://graph.facebook.com/v25.0/<WABA_ID>/subscribed_apps" -H "Authorization: Bearer $META_WHATSAPP_ACCESS_TOKEN"
   ```
   Expected: the GET lists your app id.

4. Configure the routing env for whichever branch you are testing, then
   restart `npm run dev` so the values are picked up:
   - Team-member branch (WA-03/WA-04): set `META_WHATSAPP_PHONE_NUMBER_ID`
     to the test number's phone number id, and make sure your own phone
     number is registered as a `team_members.whatsapp_number` in some agency
     (in E.164 form with a leading `+`).
   - Client-contact / unknown branch: additionally set
     `META_AGENCY_PHONE_NUMBER_MAP` to
     `{"<that same phone number id>":"<an agency id>"}`.

5. From an allowed test phone, send `hola` to the test number.

6. Observe, in order:
   - The `npm run dev` console logs an incoming POST to `/api/webhooks/meta`
     with no signature warning.
   - A `Mensaje recibido` message arrives on the sending phone.
   - Exactly ONE ack arrives, not two (idempotency under Meta's redelivery).
   - The database has both rows:
     ```bash
     psql "$DATABASE_URL" -c "select direction, resolved_identity_type, message_type, text_body, delivery_status, pricing_category, pricing_billable from messages order by created_at desc limit 4"
     ```
     Expected: an `inbound` row with the resolved identity type you set up and
     `text_body = 'hola'`, and an `outbound` row with
     `text_body = 'Mensaje recibido'`. Within a few seconds the outbound row's
     `delivery_status` becomes `sent` then `delivered`, and `pricing_category`
     is populated (expected `service`, `pricing_billable = false` — this
     phase's ack is a free Service-category reply, WA-07).

7. Send a voice note or a photo from the same phone. Expected: a row with
   `message_type` of `audio` or `image`, `media_id` populated, `text_body`
   null, and NO media download attempted (D-05).

8. Note anything that diverged from the above — especially whether Meta's
   retry behavior on the test WABA matched production expectations
   (RESEARCH.md Open Question 2), and whether the token needed regenerating
   (Pitfall 4).
  </how-to-verify>

  <acceptance_criteria>
    - Meta's "Verify and Save" succeeded for the callback URL
    - `GET /{WABA_ID}/subscribed_apps` lists the app id
    - A real WhatsApp message to the test number produced exactly one `Mensaje recibido` reply on the sender's phone
    - `messages` contains one `inbound` row with the expected `resolved_identity_type` and `text_body`, and one `outbound` row with `text_body = 'Mensaje recibido'`
    - The outbound row's `delivery_status` and `pricing_category` are populated from Meta's status receipts (WA-07)
    - A voice note or photo produced a row with `media_id` populated and `text_body` null (D-05)
    - No duplicate ack was delivered
  </acceptance_criteria>

  <verify>
    <automated>psql "$DATABASE_URL" -c "select direction, resolved_identity_type, message_type, text_body, delivery_status, pricing_category, pricing_billable from messages order by created_at desc limit 4"</automated>
  </verify>

  <done>A real WhatsApp message to the test number produced exactly one `Mensaje recibido` reply on the sender's phone, and `messages` holds the matching inbound and outbound rows with delivery status and pricing populated.</done>

  <resume-signal>Confirm the ack arrived and paste the `select ... from messages` output, then type "approved" — or describe what failed and at which step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runner to production database | `npm run db:migrate` runs DDL as the owner role |
| Verification script to real tenant data | The script seeds and deletes real rows in the shared database |
| Live Meta test WABA to the public callback URL | The first time real, unsolicited traffic reaches this endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Elevation of Privilege | SECURITY DEFINER function, live | mitigate | Assertion (3) reads `pg_proc` on the real database: `prosecdef = true`, result type `text`, and the definition contains no `role`/`email`/`RETURNS TABLE`. This is the only live proof that the escape hatch stayed narrow. |
| T-03-05 | Elevation of Privilege (SEG-12) | `messages` RLS, live | mitigate | Assertion (15) runs a real `select` inside a real `withResolvedIdentityContext` unknown scope and requires zero rows — the exact gap `02-07-SUMMARY.md` left open, closed by construction and proven here rather than by code review. |
| T-03-18 | Elevation of Privilege | Cross-tenant read by the system actor | mitigate | Assertion (17) opens `withSystemWebhookContext(agencyB)` and requires zero of agency A's rows. |
| T-03-03 | Tampering / Repudiation | Replay, live | mitigate | Assertion (10) plus the manual "exactly one ack, not two" check in Task 3 cover the database-level and the observable-behavior sides of the same guarantee. |
| T-03-26 | Denial of Service | Migration 0012 failing on existing duplicate WhatsApp numbers | mitigate | Task 2 step 2 checks for duplicates BEFORE applying the migration and instructs cleanup of stale verification data rather than dropping the index. Failing loudly is the intended behavior (DEC-B). |
| T-03-27 | Information Disclosure | Test seed data left in the shared database | mitigate | Cleanup runs in a `finally` block and deletes both seeded agencies by id; the FK cascade removes every dependent row. Test ids are prefixed `verify-whatsapp-*` so any leftovers are identifiable. |
| T-03-28 | Spoofing | Public callback URL exposed via ngrok during testing | accept | Signature verification (T-03-01) is active on that endpoint, so an unsigned request is rejected with 400. The tunnel is ephemeral and torn down after the session. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npx eslint scripts/verify-whatsapp-webhook.ts` both clean
- `npm run db:migrate` applied 0012 and 0013 to real Neon
- `npm run db:verify-whatsapp` green (21+ assertions)
- `npm run db:verify-rls` 7/7 and `npm run db:verify-identity` with no new failures
- `npx tsx scripts/verify-whatsapp-webhook-parsing.ts` and `npx tsx scripts/verify-whatsapp-send.ts` both exit 0
- A real WhatsApp message produced a real ack, confirmed by the user
</verification>

<success_criteria>
- Migrations 0012 and 0013 are applied to the real Neon database and their objects verified with `psql`
- The integration suite proves cross-agency lookup, all three identity branches, idempotency, media detection, the WA-07 pricing backfill, and all four RLS boundaries
- No Phase 1 or Phase 2 regression
- A real inbound WhatsApp message produces exactly one real outbound ack, with both rows persisted and the outbound row's delivery status and pricing populated
</success_criteria>

<output>
After completion, create `.planning/phases/03-integraci-n-con-whatsapp-meta/03-08-SUMMARY.md`
</output>
