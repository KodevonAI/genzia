---
phase: 02-modelo-identidad-permisos
plan: 07
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
files_modified: []
autonomous: false
requirements: [SEG-01, SEG-02, SEG-03, SEG-04, SEG-05, SEG-06, SEG-07, SEG-08, SEG-12]

must_haves:
  truths:
    - "Migrations 0005 through 0008 are applied to the real Neon database"
    - "The full identity-resolution suite passes against real Neon with zero failed assertions"
    - "The Phase 1 RLS isolation suite still passes after the policy changes in 0007"
    - "The phase is not marked complete on the strength of a build/typecheck alone"
  artifacts:
    - path: ".planning/phases/02-modelo-identidad-permisos/02-07-SUMMARY.md"
      provides: "Recorded evidence of the applied migrations and green suites"
  key_links:
    - from: "drizzle/migrations/0008_agent_action_catalog_seed.sql"
      to: "Neon drizzle.__drizzle_migrations"
      via: "npm run db:migrate"
      pattern: "0008_agent_action_catalog_seed"
---

<objective>
[BLOCKING] Apply Phase 2's migrations to the real Neon database and run both
verification suites against it.

Purpose: every other plan in this phase is a false-positive verification risk on its
own. `npx tsc --noEmit` and `npm run build` pass whether or not the migrations have
been applied, because the TypeScript types come from the Drizzle schema files, not from
the live database. The phase cannot pass verification until the tables, the GRANTs, the
RLS policies and the triggers actually exist in Postgres and the assertions run green
against them.

Output: green `db:verify-identity` and `db:verify-rls` runs, recorded in the SUMMARY.
No source files change in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-modelo-identidad-permisos/02-VALIDATION.md

@scripts/migrate.ts
@drizzle.config.ts
@package.json
@drizzle/migrations/meta/_journal.json

<interfaces>
Migration path for this project (verified from package.json and drizzle.config.ts —
this project does NOT use `drizzle-kit push`):
  1. `npx drizzle-kit generate`  — offline schema diff, writes a numbered .sql + snapshot
  2. `npm run db:migrate`        — `tsx scripts/migrate.ts`, applies via neon-http against real Neon

Suites:
  npm run verify:identity-classification  — pure, no network, sandbox-safe
  npm run db:verify-identity              — real Neon, local/CI only
  npm run db:verify-rls                   — real Neon, Phase 1 regression, local/CI only

Environment gate (STATE.md): a sandboxed session has NO network egress to Neon.
`npm run db:migrate`, `db:verify-identity` and `db:verify-rls` all fail there with a
connection error. Phase 1's 01-03 and 01-04 hit this exact gate and were verified from
the user's own machine instead — the same applies here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm the migration set is complete and consistent</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    - drizzle/migrations/meta/_journal.json — must contain 9 entries, idx 0 through 8, tags 0000..0008 in order
    - drizzle.config.ts — confirms `generate` needs no live connection
    - drizzle/migrations/0006_phase2_identity_rls.sql, 0007_client_contact_scope.sql, 0008_agent_action_catalog_seed.sql — the hand-authored files about to be applied
  </read_first>
  <action>
Run these checks offline, before touching the database. All are sandbox-safe.

1. Journal integrity — the entries array must be contiguous and end at 0008:

       node -e "const j=require('./drizzle/migrations/meta/_journal.json'); j.entries.forEach((e,i)=>{if(e.idx!==i) throw new Error('journal idx gap at '+i)}); console.log(j.entries.map(e=>e.tag).join('\n'))"

   Expected output ends with `0005_phase2_identity_tables`, `0006_phase2_identity_rls`,
   `0007_client_contact_scope`, `0008_agent_action_catalog_seed`.

2. Every journal tag has a matching `.sql` file:

       node -e "const fs=require('fs'); const j=require('./drizzle/migrations/meta/_journal.json'); j.entries.forEach(e=>{const p='drizzle/migrations/'+e.tag+'.sql'; if(!fs.existsSync(p)) throw new Error('missing '+p)}); console.log('all migration files present')"

3. No pending schema drift — the Drizzle schema and the snapshots must already agree:

       npx drizzle-kit generate

   Expected: it reports no schema changes and creates NO new file. If it DOES create a
   new migration, STOP: a schema file changed after plan 02-02 generated 0005. Delete
   the stray file and its journal entry, reconcile the schema, and report before
   continuing.

4. Both offline gates green: `npx tsc --noEmit`, `npm run lint`,
   `npm run verify:identity-classification`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run verify:identity-classification</automated>
  </verify>
  <acceptance_criteria>
    - the journal integrity command prints 9 tags ending in `0008_agent_action_catalog_seed` and throws no error
    - the file-existence command prints `all migration files present`
    - `npx drizzle-kit generate` reports no schema changes and `git status --porcelain drizzle/migrations` is empty afterwards
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `npm run verify:identity-classification` exits 0 with `All assertions passed.`
  </acceptance_criteria>
  <done>All four migrations present and consistent, no schema drift, all offline gates green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migrations 0005-0008 to Neon and run the live suites</name>
  <files>(no file changes — database state only)</files>
  <read_first>
    - scripts/migrate.ts — explains why this project applies migrations via the neon-http migrator rather than `drizzle-kit migrate`, and that DATABASE_URL must be the OWNER role here, not `app_user` (app_user has no DDL grants, by design, per 0001_rls_policies.sql)
    - .planning/STATE.md — the sandboxed-session network gate and how Phase 1's 01-03/01-04 handled it
    - drizzle/migrations/0001_rls_policies.sql lines 22-42 — why the APPLICATION's DATABASE_URL must be `app_user`, or `FORCE ROW LEVEL SECURITY` has no effect and every isolation assertion would pass for the wrong reason
  </read_first>
  <what-built>
Plans 02-01 through 02-06 produced, but did not apply:
- migration 0005: `authorized_contacts`, `agent_action_catalog`, `audit_log` tables
- migration 0006: GRANTs to `app_user`, RLS on the two tenant tables, both phone-collision triggers
- migration 0007: the `client_contact` branch on `clients_select_by_role` (new `app.client_id` GUC) plus the `client_contact` lockout on `agencies`, `team_members`, `client_assignments`, `agent_brand_config`
- migration 0008: D-02's three seeded action types
- `lib/identity/*`, `lib/tenant/with-resolved-identity-context.ts`, `lib/clients/manage-authorized-contacts.ts`
- `scripts/verify-identity-resolution.ts` — written but never executed

A sandboxed session has no network egress to Neon (STATE.md), so nothing above has
touched the real database. Build and typecheck pass regardless, which is exactly the
false-positive state this checkpoint exists to prevent.
  </what-built>
  <how-to-verify>
Run these from a machine with Neon access (the user's own machine or CI), from the repo
root, in this order. Stop at the first failure and report it rather than continuing.

1. Confirm `DATABASE_URL` in the migrating shell is the Neon OWNER role (the one that
   ran 0000), not `app_user` — `scripts/migrate.ts` needs DDL rights:

       psql "$DATABASE_URL" -c "select current_user"

2. Apply the migrations:

       npm run db:migrate

   Expected: prints `Migrations applied.` and exits 0. It applies only 0005-0008; the
   earlier four are already recorded in `drizzle.__drizzle_migrations`.

3. Confirm all four landed:

       psql "$DATABASE_URL" -c "select hash, created_at from drizzle.__drizzle_migrations order by created_at desc limit 4"
       psql "$DATABASE_URL" -c "\d+ authorized_contacts"
       psql "$DATABASE_URL" -c "select code, risk_level from agent_action_catalog order by code"

   Expected from the last one, exactly three rows:
       new_client_content     | high
       payment_reminder       | low
       reschedule_appointment | high

4. Confirm the policies exist and the catalog has none:

       psql "$DATABASE_URL" -c "select tablename, policyname from pg_policies where schemaname='public' order by tablename, policyname"

   Expected to include `authorized_contacts_select_team_only`,
   `authorized_contacts_write_admin_only`, `audit_log_tenant_isolation`,
   `clients_select_by_role`, `clients_write_admin_only`, and the four
   `*_tenant_isolation` policies. `agent_action_catalog` must appear ZERO times.

5. Switch `DATABASE_URL` back to the `app_user` connection string (the application's
   own, non-owner role — otherwise FORCE RLS is bypassed and every assertion below
   passes for the wrong reason), then run:

       npm run db:verify-rls

   Expected: `All assertions passed.` — this is the Phase 1 regression gate proving
   migration 0007's added `<> 'client_contact'` clauses did not break existing access.

6. Still as `app_user`:

       npm run db:verify-identity

   Expected: `All assertions passed.` with zero `[FAIL]` lines, covering the four
   ROADMAP cases, cross-client isolation, the roster/team lockout, the opt-in CHECK,
   both collision triggers and the uuid-GUC regression.

7. Confirm cleanup left nothing behind:

       psql "$DATABASE_URL" -c "select count(*) from agencies where id like 'verify-%'"

   Expected: 0.

Report back with: the output of step 3's catalog query, the pass/fail line counts from
steps 5 and 6, and step 7's count. If any assertion fails, paste the `[FAIL]` lines —
Phase 1 found five real bugs exactly this way, so a failure here is the expected value
of this checkpoint, not an exception.
  </how-to-verify>
  <acceptance_criteria>
    - `npm run db:migrate` exits 0 and prints `Migrations applied.`
    - `select code, risk_level from agent_action_catalog` returns exactly the three D-02 rows with `payment_reminder` = `low`
    - `pg_policies` lists `authorized_contacts_select_team_only`, `authorized_contacts_write_admin_only` and `audit_log_tenant_isolation`, and zero rows for `agent_action_catalog`
    - `npm run db:verify-rls` exits 0 with `All assertions passed.` (Phase 1 regression)
    - `npm run db:verify-identity` exits 0 with `All assertions passed.` and zero `[FAIL]` lines
    - `select count(*) from agencies where id like 'verify-%'` returns 0
  </acceptance_criteria>
  <resume-signal>Paste the outputs listed in step 7 of how-to-verify, then type "approved" — or paste the failing `[FAIL]` lines.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the evidence and close the phase loop</name>
  <files>.planning/phases/02-modelo-identidad-permisos/02-07-SUMMARY.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/01-fundaciones-cuenta-equipo/01-fundaciones-cuenta-equipo-01-SUMMARY.md — the SUMMARY structure and the "Authentication / Environment Gates" section Phase 1 used to record exactly this kind of network-gated verification
    - .planning/STATE.md — the sections to update: "Dónde vamos", "Completado", "Continuidad de sesión"
    - .planning/phases/02-modelo-identidad-permisos/02-VALIDATION.md — the sign-off checklist
  </read_first>
  <action>
Write `.planning/phases/02-modelo-identidad-permisos/02-07-SUMMARY.md` recording:
- Which migrations were applied and the verbatim outputs from Task 2's steps 3, 5, 6 and 7
- Any real bug the live run surfaced, with the fix — Phase 1 found five this way; if
  this run found zero, say so explicitly rather than leaving it implied
- The environment gate: which commands could not run in-session and where they were run
  instead, matching Phase 1's precedent

Then update `.planning/STATE.md`:
- "Dónde vamos": Fase 2 complete, verified end to end against real Neon
- "Completado": one bullet per plan 02-01..02-07 with its verified outcome
- "Decisiones clave que no deben perderse": add the three-branch RLS convention
  (admin / assigned-member / client_contact) as the contract every future
  client-scoped table must repeat, and the `NULLIF(current_setting(...), '')::uuid`
  rule for every new uuid-typed GUC
- "Continuidad de sesión": next step is `/gsd-verify-work 2` or Fase 3

Do NOT mark the phase complete if any assertion in Task 2 failed.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-modelo-identidad-permisos/02-07-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `02-07-SUMMARY.md` exists and contains the literal strings `db:verify-identity`, `db:verify-rls` and `Migrations applied.`
    - it contains a section documenting the sandbox network gate and where the live run happened
    - `.planning/STATE.md` contains `Fase 2` marked complete and a bullet mentioning the three-branch RLS convention
    - `.planning/STATE.md` contains the string `NULLIF(current_setting`
    - `git status --porcelain lib/ drizzle/ scripts/` is empty (this plan changes no source)
  </acceptance_criteria>
  <done>Evidence recorded, STATE.md updated with the durable conventions, no source drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration shell → Neon | Runs as the owner role with DDL rights; the only path by which policies come into existence |
| Application connection → Neon | Must be `app_user`, a non-owner role, or FORCE RLS is silently bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | Phase marked verified while migrations were never applied (build/typecheck pass regardless) | mitigate | This entire plan is the [BLOCKING] gate; Task 2's acceptance criteria require live `psql` and suite output, not a build result. |
| T-02-15 | Information Disclosure | Suites run as the table OWNER, where Postgres exempts the role from RLS unconditionally, so every isolation assertion passes for the wrong reason | mitigate | Step 1 checks `current_user` before migrating; step 5 explicitly requires switching `DATABASE_URL` back to `app_user` before running either suite — the same check `verify-rls-isolation.ts` prints in its own header. |
| T-02-01 | Information Disclosure | Migration 0007's added clauses breaking Phase 1 access, or failing to block client_contact | mitigate | Both suites are required green: `db:verify-rls` is the Phase 1 regression, `db:verify-identity` is the new coverage. |
| T-02-13 | Tampering | Test fixtures left on a shared Neon branch | mitigate | Step 7 asserts `count(*) = 0` for `verify-%` agencies after the run. |
</threat_model>

<verification>
1. `npm run db:migrate` applied 0005-0008 with no error
2. `npm run db:verify-rls` green as `app_user` (Phase 1 regression intact)
3. `npm run db:verify-identity` green as `app_user`, zero `[FAIL]` lines
4. `agent_action_catalog` holds exactly D-02's three rows and has zero RLS policies
5. No `verify-%` fixture agencies remain
6. `npm run verify:identity-classification`, `npx tsc --noEmit`, `npm run lint` all green
</verification>

<success_criteria>
- Migrations 0005 through 0008 are recorded in `drizzle.__drizzle_migrations`
- Both live suites pass from a network-capable environment, run as `app_user`
- Evidence is recorded in the SUMMARY, including any bug found and fixed
- STATE.md carries forward the three-branch RLS convention and the NULLIF-GUC rule
</success_criteria>

<output>
After completion, create `.planning/phases/02-modelo-identidad-permisos/02-07-SUMMARY.md`
</output>
